### `CoordinateSystems.py` — Defines useful extended internal coordinate frames
  - **class `MolecularEmbedding`**
    - `__init__(masses, coords, internals, internal_fd_opts=None, cartesian_fd_opts=None)`
    - `get_direct_converter(target)` — Provide a converter from this embedding's coordinate system directly to plain (non-molecular) 3D Ca…
    - `get_inverse_converter(target)` — Provide a converter from plain (non-molecular) 3D Cartesian coordinates into this embedding's coord…
    - `cleanup()` — Deregister every converter previously registered via `register`, if any.
    - `register()` — Register the Cartesian coordinate converters for this embedding's coordinate system with the global…
    - `coords()` — Property getter/setter for the Cartesian coordinates.
    - `coords(coords)` — Property getter/setter for the Cartesian coordinates.
    - `masses()` — The atomic masses, taken from the Cartesian coordinate system.
    - `canonicalize_internal_coordinate_spec(spec)` — Normalize the many accepted forms of an internal-coordinate specification (an options dict with `'z…
    - `internals()` — Property getter/setter for the raw (canonicalized) internal-coordinate specification.
    - `internals(internals)` — Property getter/setter for the raw (canonicalized) internal-coordinate specification.
    - `zmatrix()` — Property getter/setter for just the Z-matrix ordering array out of the internal-coordinate specific…
    - `zmatrix(zmat)` — Property getter/setter for just the Z-matrix ordering array out of the internal-coordinate specific…
    - `convert_to_internals(coords, masses, spec)` — Build the internal-coordinate `CoordinateSet` described by `spec`: constructs (and registers conver…
    - `internal_coordinates_from_spec(spec)` — Build the internal coordinates for this embedding's current Cartesian coordinates and masses from a…
    - `internal_coordinates()` — Property getter/setter for the internal coordinates.
    - `internal_coordinates(ics)` — Property getter/setter for the internal coordinates.
    - `strip_embedding_coordinates(coords)` — Drop the fixed embedding coordinates (e.g.
    - `strip_derivative_embedding(derivs)` — Drop the fixed embedding coordinates from every axis of each tensor in a list of Cartesian-derivati…
    - `restore_embedding_coordinates(coords)` — Reinsert the fixed embedding coordinates (filled in from the reference internal coordinates) back i…
    - `restore_derivative_embedding(derivs)` — Reinsert zeroed-out placeholder entries for the fixed embedding coordinates back into every axis of…
    - `get_internals(*, coords=None, strip_embedding=True)` — Fetch the internal coordinates, either the cached ones for this embedding's own geometry or freshly…
    - `get_cartesians(*, coords=None, strip_embedding=True)` — Fetch Cartesian coordinates, either this embedding's own cached ones or the Cartesian coordinates c…
    - `redundant_internal_transformation()` — The redundant-to-non-redundant transformation matrix associated with the current internal coordinat…
    - `embedding_coords()` — The indices of the internal-coordinate system's fixed embedding coordinates (translation/rotation d…
    - `get_cartesians_by_internals(order=None, strip_embedding=False, reembed=True, method=None, coords=None, **fd_opts)` — Compute the Cartesians-by-internals Jacobian expansion up to the requested `order`, either via the…
    - `get_internals_by_cartesians(order=None, strip_embedding=False, coords=None, **opts)` — Compute the internals-by-Cartesians Jacobian expansion up to the requested `order`, via finite diff…
    - `embed_coords(coords, sel=None, in_paf=False, planar_ref_tolerance=None, proper_rotation=True)` — Eckart-embed a set of Cartesian coordinates onto this embedding's reference geometry.
    - `unembed_derivs(coords, derivs, sel=None, in_paf=False, planar_ref_tolerance=None)` — Undo an Eckart embedding's rotation on a set of Cartesian derivative tensors, transforming them bac…
    - `inertia_tensor()` — The molecule's inertia tensor at its current Cartesian coordinates.
    - `inertial_frame()` — Provides the inertial axis frame
    - `inertial_frame_derivatives()` — The first and second derivatives of the inertia tensor with respect to mass-weighted Cartesian disp…
    - `translation_rotation_modes()` — The (cached) translation and rotation eigenvectors of the molecule at its current geometry.
    - `get_translation_rotation_invariant_transformation(order=0, mass_weighted=True, strip_embedding=True, coords=None)` — Build the transformation (and its inverse) that projects out the translational and rotational degre…
  - **class `ModeEmbedding`**
    > Provides a specialization on a `MoleculaEmbedding` to express all properties
    > in terms of the attendant normal modes
    - `__init__(embedding, modes, mass_weight=None, dimensionless=None, masses=None)`
    - `mw_conversion(strip_dummies=None)` — Build the diagonal mass-weighting matrix (`sqrt(mass)` per Cartesian coordinate) used to convert pl…
    - `mw_inverse(strip_dummies=None)` — Build the diagonal inverse-mass-weighting matrix (`1/sqrt(mass)` per Cartesian coordinate) used to…
    - `get_mw_cartesians_by_internals(order=None, mass_weighted=None, coords=None, strip_embedding=True)` — Fetch the Cartesians-by-internals Jacobian expansion, converted to mass-weighted Cartesians if `mas…
    - `get_internals_by_mw_cartesians(order=None, mass_weighted=None, coords=None, strip_embedding=True)` — Fetch the internals-by-Cartesians Jacobian expansion, converted to be with respect to mass-weighted…
    - `get_internals_by_cartesians(order=None, coords=None, strip_embedding=True)` — expresses raw internals or modes (internals or Cartesian) in terms of mass-weighted Cartesians
    - `get_cartesians_by_internals(order=None, coords=None, strip_embedding=True)` — expresses raw internals or modes (internals or Cartesian) in terms of mass-weighted Cartesians
    - `get_inertia_tensor_expansion(order=None, strip_embedding=True)` — Compute the Taylor expansion of the inertia tensor in terms of this embedding's coordinates (intern…
    - `get_inertial_frame()` — The molecule's inertial (principal-axis) frame, delegated to the underlying embedding.
    - `get_modes_by_coords(mass_weighted=None, frequency_scaled=None)` — The modes-by-coordinates transformation matrix, optionally adjusting the mass-weighting/frequency-s…
    - `get_coords_by_modes(mass_weighted=None, frequency_scaled=None)` — The coordinates-by-modes transformation matrix, optionally adjusting the mass-weighting/frequency-s…
  - **class `MolecularGenericInternalCoordinateSystem`** (GenericInternalCoordinateSystem)
    > Mirrors the standard ZMatrix coordinate system in _almost_ all regards, but forces an embedding
    - **class `PassThroughRedundantGenerator`**
      - `__init__(redundant_transformation, redundant_inverse=None, masses=None, untransformed_coordinates=None, relocalize=False, **opts)`
      - `get_redundant_transformation(base_expansions, **ignored)` — Return the stored redundant transformation unchanged, along with the base derivative expansions re-…
    - `__init__(self, masses, coords, /, specs, converter_options=None, redundant=False, relocalize=False, untransformed_coordinates=None, redundant_transformation=None, redundant_inverse=None, angle_ordering='ijk', **opts)`
  - **class `MolecularZMatrixCoordinateSystem`** (ZMatrixCoordinateSystem)
    > Mirrors the standard ZMatrix coordinate system in _almost_ all regards, but forces an embedding
    - `__init__(masses, coords, converter_options=None, **opts)`
    - `origins()` — The Z-matrix embedding's origin points (typically the reference center of mass), from `converter_op…
    - `axes()` — The Z-matrix embedding's reference axes (typically two principal axes), from `converter_options['ax…
    - `get_direct_converter(target)` — Provide a converter from this molecular Z-matrix system directly to the plain (non-molecular) `ZMat…
    - `get_inverse_converter(target)` — Provide a converter from the plain `ZMatrix` coordinate system into this molecular Z-matrix system,…
    - `pre_convert_to(system, opts=None)` — Re-establish the embedding options (via `set_embedding`) before delegating to the base class's `pre…
    - `set_embedding()` — (Re)compute and store this Z-matrix system's embedding options -- the reference origin, reference a…
    - `jacobian(coords, *args, reembed=None, strip_dummies=None, converter_options=None, **kwargs)` — Compute the Jacobian of this Z-matrix system with respect to Cartesian coordinates, handling batche…
  - **class `MolecularCartesianCoordinateSystem`** (CartesianCoordinateSystem)
    > Mirrors the standard Cartesian coordinate system in _almost_ all regards, but forces an embedding
    - `__init__(masses, coords, dummy_positions=None, converter_options=None, **opts)`
    - `to_state(serializer=None)` — Serialize this coordinate system's state, adding the masses, coordinates, and dummy-atom positions…
    - `from_state(data, serializer=None)` — Reconstruct a `MolecularCartesianCoordinateSystem` from a previously serialized state dict.
    - `pre_convert_to(system, opts=None)` — Ensure the masses are up to date in `converter_options`, and, if converting to a Z-matrix-family sy…
    - `set_embedding()` — Sets up the embedding options...
    - `jacobian(coords, system, order=None, strip_dummies=None, converter_options=None, analytic_deriv_order=None, **kwargs)` — Compute the Jacobian of these Cartesian coordinates with respect to `system`, resolving the analyti…
  - **class `MolecularCartesianToZMatrixConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(cart_system, zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, *, masses, dummy_positions, origins=None, axes=None, ordering=None, **kwargs)` — Converts from Cartesian to ZMatrix coords, preserving the embedding
    - `convert_many(coords, *, masses, dummy_positions, origins=None, axes=None, ordering=None, strip_embedding=True, strip_dummies=False, return_derivs=None, **kwargs)` — Converts from Cartesian to ZMatrix coords, preserving the embedding
  - **class `MolecularCartesianToRegularCartesianConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(cart_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a molecular Cartesian system and a plain Cartesian sy…
    - `convert_many(coords, **kwargs)` — Converts from Cartesian to ZMatrix coords, preserving the embedding
  - **class `RegularCartesianToMolecularCartesianConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(cart_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a plain Cartesian system and a molecular Cartesian sy…
    - `convert_many(coords, **kwargs)` — Converts from Cartesian to ZMatrix coords, preserving the embedding
  - **class `MolecularZMatrixToCartesianConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(zmat_system, cart_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Convert a single frame of Z-matrix coordinates to Cartesians by delegating to `convert_many` on a l…
    - `convert_many(coords, *, masses, dummy_positions, ref_coords, origins=None, axes=None, ordering=None, reembed=False, axes_choice=None, return_derivs=None, strip_dummies=False, strip_embedding=True, planar_ref_tolerance=None, embedding_masses=None, spec=None, **kwargs)` — Converts from Cartesian to ZMatrix coords, attempting to preserve the embedding
  - **class `MolecularZMatrixToRegularZMatrixConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a molecular Z-matrix system and a plain Z-matrix syst…
    - `convert_many(coords, **kwargs)` — Pass a batch of coordinates through unchanged, since a molecular Z-matrix system and a plain Z-matr…
  - **class `RegularZMatrixToMolecularZMatrixConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a plain Z-matrix system and a molecular Z-matrix syst…
    - `convert_many(coords, **kwargs)` — Pass a batch of coordinates through unchanged, since a plain Z-matrix system and a molecular Z-matr…
  - **class `MolecularCartesianToGICConverter`** (CartesianToGICSystemConverter)
    > ...
    - `__init__(cart_system, zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert_many(coords, *, order=0, return_derivs=None, redundant_generator=None, reference_internals=None, redundant_transformation=None, handle_periodicity=True, **kw)` — We'll implement this by having the ordering arg wrap around in coords?
  - **class `MolecularGICToCartesianConverter`** (GICSystemToCartesianConverter)
    > ...
    - `__init__(cart_system, zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert_many(coords, *, redundant_transformation=None, redundant_inverse=None, redundant_generator=None, reference_internals=None, **kw)` — Convert a batch of generic-internal coordinates to Cartesians, forwarding any redundant transformat…
  - **class `RegularGICToMolecularGICConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(**opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a plain generic-internal system and a molecular gener…
    - `convert_many(coords, **kwargs)` — Pass a batch of coordinates through unchanged, since a plain generic-internal system and a molecula…
  - **class `MolecularGICConverterToRegularGIC`** (CoordinateSystemConverter)
    > ...
    - `__init__(**opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a molecular generic-internal system and a plain gener…
    - `convert_many(coords, **kwargs)` — Pass a batch of coordinates through unchanged, since a molecular generic-internal system and a plai…
  - **class `MolecularIZCoordinateSystem`** (MolecularZMatrixCoordinateSystem)
  - **class `MolecularCartesianToIZConverter`** (MolecularCartesianToZMatrixConverter)
    > ...
  - **class `MolecularIZToCartesianConverter`** (MolecularZMatrixToCartesianConverter)
    > ...
    - `convert_many(coords, *, masses, dummy_positions, ref_coords, origins=None, axes=None, ordering=None, reembed=False, axes_choice=None, return_derivs=None, strip_dummies=False, strip_embedding=True, planar_ref_tolerance=None, embedding_masses=None, fixed_atoms=None, fixed_coords=None, **kwargs)` — Convert a batch of iterative-Z-matrix coordinates to Cartesians, filling in sensible defaults for t…
  - **class `RegularIZToMolecularIZConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a plain iterative-Z-matrix system and a molecular one…
    - `convert_many(coords, **kwargs)` — Pass a batch of coordinates through unchanged, since a plain iterative-Z-matrix system and a molecu…
  - **class `MolecularIZToRegularIZConverter`** (CoordinateSystemConverter)
    > ...
    - `__init__(zmat_system, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Pass the coordinates through unchanged, since a molecular iterative-Z-matrix system and a plain one…
    - `convert_many(coords, **kwargs)` — Pass a batch of coordinates through unchanged, since a molecular iterative-Z-matrix system and a pl…

### `Evaluator.py`
  - **class `MolecularEvaluator`**
    - `__init__(embedding, normal_modes)`
    - `coords()` — The Cartesian coordinates, delegating to `self.embedding.coords`.
    - `masses()` — The atomic masses, delegating to `self.embedding.masses`.
    - `evaluate(func, use_internals=None, order=None, strip_embedding=False)` — Evaluate a coordinate function (and, optionally, its derivatives) either directly in Cartesian coor…
    - `evaluate_at(func, coords, use_internals=None, order=None, strip_embedding=False)` — Evaluate a coordinate function at an alternate set of coordinates, by building a fresh `MolecularEv…
    - `get_displaced_coordinates(displacements, which=None, sel=None, axes=None, use_internals=False, coordinate_expansion=None, expansion_active_positions=None, strip_embedding=False, shift=True, coords=None)` — Build displaced coordinates from a reference geometry by applying `displacements` -- either directl…
    - `get_scan_coordinates(domains, internals=False, which=None, sel=None, axes=None, coordinate_expansion=None, strip_embedding=False, shift=True, return_displacements=False)` — Build a grid of displaced coordinates spanning the given `(start, stop, num)` domains for each scan…
    - `get_nearest_displacement_atoms(points, sel=None, axes=None, weighting_function=None, return_distances=False)` — For each of a set of query points, find the atom (restricted to `sel`, along `axes`) whose (optiona…
    - `get_nearest_displacement_coordinates(points, sel=None, axes=None, weighting_function=None, modes_nearest=False, return_distances=False)` — Displaces the _nearest_ atom (in a mass-weighted sense) to the given point
    - `get_nearest_scan_coordinates(domains, sel=None, axes=None)` — Build a grid of "nearest displacement" coordinates (see `get_nearest_displacement_coordinates`) spa…
  - **class `InternalHandlingMode`** (enum.Enum)
    - `resolve(mode)` — Normalize a user-supplied internal-coordinate handling flag into an `InternalHandlingMode` enum val…
  - **class `PropertyEvaluator`**
    - `__init__(embedding=None, permutation=None, use_internals=True, strip_embedding=True, flatten_internals=True, reembed_cartesians=False, supports_internals=None, **defaults)`
    - `use_internal_coordinate_handlers()` — Whether this evaluator should route coordinate handling through the internal-coordinate machinery:…
    - `embed_coords(coords, embed_reembedded=True)` — Convert raw Cartesian coordinates into whatever representation this evaluator actually evaluates in…
    - `unembed_coords(coords, embed_reembedded=True)` — Undo `embed_coords`: convert the evaluator's working coordinate representation back to plain Cartes…
    - `embed_derivs(coords, derivs, embed_reembedded=True)` — Re-express a set of derivative tensors (computed with respect to the evaluator's working coordinate…
    - `unembed_derivs(base_coords, coords, derivs, embed_reembedded=True)` — Undo `embed_derivs` (and any atom/coordinate permutation) on a set of Cartesian-coordinate derivati…
    - `evaluate_term(coords, order, **opts)` — Abstract hook for evaluating the property's value (or a specific analytic derivative order) at the…
    - `prep_mol_opts(mol, embedding=None, charge=None, multiplicity=None, **opts)` — Build the keyword-argument dict used to construct an evaluator from a molecule, pulling the embeddi…
    - `from_mol(mol, **opts)` — Abstract constructor for building an evaluator instance from a molecule.
    - `get_fd_opts(**opts)` — Merge the class-level `fd_defaults` with any explicitly passed finite-difference options.
    - `finite_difference_derivs(coords, order, batched_orders=None, displacement_generator=None, coordinate_prep=None, index_filter=None, analytic_derivative_order=None, **opts)` — Compute derivative tensors of the property numerically via finite differences in the evaluator's wo…
    - `internal_finite_difference_derivs(coords, order, batched_orders=None, displacement_generator=None, coordinate_prep=None, index_filter=None, analytic_derivative_order=None, **opts)` — Compute derivative tensors of the property numerically via finite differences directly in internal…
    - `evaluate_expansion(coords, order=0, analytic_derivative_order=None, batched_orders=None, logger=None, fd_displacement_generator=None, fd_coordinate_prep=None, fd_index_filter=None, fd_handler=None, **opts)` — Build the property's Taylor expansion up to the requested derivative order(s), combining analytic d…
    - `evaluate(coords, order=0, logger=None, fd_handler=None, analytic_derivative_order=None, unembed_derivatives=True, **opts)` — Top-level property-evaluation entry point: embeds the input coordinates into the evaluator's workin…
    - `partial_force_field(coords, modes, mode_distance_units='BohrRadius', analytic_derivative_order=None, order=4, index_filter=None, fd_handler=None, mesh_spacing=0.5, **opts)` — Compute a partial force-field-style expansion of the property restricted to displacements along a g…
    - `register(name, method=None)` — Register a named evaluator-construction method (or function) in the class's `evaluator_registry`, u…
    - `get_evaluators()` — The set of built-in named evaluator constructors for this evaluator type.
    - `get_evaluator_map()` — The full set of named evaluator constructors available for this evaluator type: the built-ins from…
    - `get_evaluators_by_attributes()` — Optional mapping used to resolve an evaluator by a set of attribute-based hints rather than by exac…
    - `profile_generator_dispatch()` — Lazily build (and cache) the `OptionsMethodDispatch` object used to resolve a named/attribute-descr…
    - `get_default_function_evaluator_type()` — Abstract hook returning the `PropertyFunctionEvaluator`-family class to use when a bare callable is…
    - `handle_specialization(tag)` — Hook for resolving an evaluator "specialization" tag (the part after a `:` in an evaluator name str…
    - `resolve_evaluator(name)` — Resolve an evaluator specification -- a bare callable, an object already exposing `evaluate`, or a…
    - **class `quiet_mode`**
      - `__init__(quiet=True)`
  - **class `PropertyFunctionEvaluator`** (PropertyEvaluator)
    - `__init__(potential_function, property_units=None, distance_units='Angstroms', batched_orders=False, analytic_derivative_order=np.inf, **defaults)`
    - `evaluate_term(coords, order, **opts)` — Evaluate the wrapped function directly at the given coordinates/order.
    - `bind_default(potential)` — Register a default property function to be used the next time `initialize_from_mol` is called witho…
    - `get_property_function(prop_func, mol, **opts)` — Resolve the property function to wrap.
    - `initializer(mol, **opts)` — Build a bare (function-less) instance of this evaluator type; used as a lightweight constructor hoo…
    - `initialize_from_mol(cls, mol, property_function=None, *, embedding=None, use_internals=None, reembed_cartesians=None, property_units=None, distance_units=None, batched_orders=None, analytic_derivative_order=None, **opts)` — Build a `PropertyFunctionEvaluator` for a molecule, resolving the property function to wrap (fallin…
  - **class `EnergyEvaluator`** (PropertyEvaluator)
    - `get_evaluators_by_attributes()` — Attribute-based dispatch mapping for energy evaluators: recognizes a `potential_function` keyword a…
    - `get_default_function_evaluator_type()` — The evaluator class used when a bare callable is supplied as an energy-evaluator specification.
    - `to_ase(gradient_modification_function=None, gradient_modification_mode=None, convert_modification_distances=None, convert_modification_energies=None, orthogonal_projection_generator=None, **etc)` — Wrap this evaluator as an ASE `Calculator` object (via `ASECalculator`), optionally intercepting/mo…
    - `to_pysis(gradient_modification_function=None, gradient_modification_mode=None, convert_modification_distances=None, convert_modification_energies=None, orthogonal_projection_generator=None, **etc)` — Wrap this evaluator as a pysisyphus calculator (via `PysisCalculator`), optionally intercepting/mod…
    - `minimizer_function_by_order(order, allow_fd=False, modifier=None, **opts)` — Build a callable of the `scipy.optimize`-style `(coords, *extra) -> value` form for a specific deri…
    - `minimizer_func(**opts)` — Build a `scipy.optimize`-compatible energy-evaluation function, via `minimizer_function_by_order(0,…
    - `minimizer_jacobian(**opts)` — Build a `scipy.optimize`-compatible gradient-evaluation function (falling back to finite difference…
    - `minimizer_hessian(**opts)` — Build a `scipy.optimize`-compatible Hessian-evaluation function, via `minimizer_function_by_order(2…
    - `minimizer_internal_function_by_order(order, allow_fd=False, modifier=None, **opts)` — Like `minimizer_function_by_order`, but for evaluating in internal coordinates: when re-embedding,…
    - `get_internal_coordinate_indices(coord_spec)` — Resolve which internal-coordinate indices (and the total internal-coordinate dimension) correspond…
    - `get_internal_coordinate_projector(coord_spec, mask=None)` — Build a constraint function returning the orthogonal-projection matrix that removes the directions…
    - `get_internal_coordinate_constraints(constraints)` — Resolve a constraints specification for internal coordinates into a projector-generating constraint…
    - `get_optimizer_options()` — The full set of recognized keyword-argument names for `optimize`/`optimize_iterative`/`relaxed_scan…
    - `get_coordinate_projector(coord_spec, region_constraints=None, mask=None)` — Build a constraint function returning the orthogonal-projection matrix that removes the directions…
    - `get_coordinate_constraints(constraints, region_constraints=None)` — Resolve a constraints specification for general (Cartesian-space) coordinates into a projector-gene…
    - `optimize_iterative(coords, coordinate_constraints=None, gradient_modification_function=None, gradient_modification_mode='shift', convert_modification_distances=True, convert_modification_energies=True, return_trajectory=False, initialization_function=None, line_search_step=None, **opts)` — Core geometry-optimization driver supporting several backends (a custom iterative step-minimization…
    - `optimize(coords, use_internals=None, func=None, jacobian=None, hessian=None, logger=None, coordinate_constraints=None, orthogonal_projection_generator=None, gradient_modification_function=None, initialization_function=None, return_trajectory=False, mode=None, **opts)` — Geometry-optimize a structure, automatically routing through internal coordinates (building the app…
    - `get_relaxed_scan_options()` — The full set of recognized keyword-argument names for `relaxed_scan`, extending `get_optimizer_opti…
    - `relaxed_scan(coords, displacement_specs, displacement_coords, coordinate_constraints=None, absolute_mesh=False, orthogonal_projection_generator=None, coordinate_expansion=None, expansion_active_positions=None, include_displacement_constraints=True, shift=True, adjust_displacements=True, split_scan_mesh=True, **optimization_settings)` — Perform a constrained ("relaxed") potential-energy scan: for each point on a mesh of displacement v…
  - **class `RDKitEnergyEvaluator`** (EnergyEvaluator)
    - `__init__(rdmol, force_field='mmff', charge=None, multiplicity=None, **defaults)`
    - `from_mol(mol, **opts)`
    - `evaluate_term(coords, order, force_field_type=None, **opts)`
    - `optimize(coords, method=None, force_field_type=None, unitary=False, tol=1e-08, max_iterations=100, **opts)`
  - **class `AIMNet2EnergyEvaluator`** (EnergyEvaluator)
    > Borrows structure from AIMNet2ASE to call appropriately
    - `__init__(atoms, model='aimnet2', charge=0, device=None, model_dir=None, multiplicity=None, quiet=True, **defaults)`
    - `handle_specialization(tag)`
    - `from_mol(mol, **opts)`
    - `to_ase(gradient_modification_function=None, gradient_modification_mode='shift', convert_modification_distances=True, convert_modification_energies=True, orthogonal_projection_generator=None, **etc)`
    - `to_pysis(gradient_modification_function=None, gradient_modification_mode='shift', convert_modification_distances=False, convert_modification_energies=False, orthogonal_projection_generator=None, **etc)`
    - `setup_aimnet(model, device=None, model_dir=None)`
    - `autodiff(expr, coord, pad_dim, create_graph=False, retain_graph=False)`
    - `prep_eval(coords, order, keep_graph=None, forces=None, hessian=None, **opts)`
    - `process_aimnet_derivs(base_shape, coords, data, root_key, order, extra_deriv_coord=None, deriv_key=None, property_shape=(), pad_coord=1, replace_nans=True, output_shape=None)`
    - `evaluate_term(coords, order, **opts)`
  - **class `ASECalcEnergyEvaluator`** (EnergyEvaluator)
    - `__init__(atoms, charge=0, multiplicity=1, quiet=True, embedding=None, **defaults)`
    - `to_ase(gradient_modification_function=None, gradient_modification_mode='shift', convert_modification_distances=True, convert_modification_energies=True, orthogonal_projection_generator=None, **etc)`
    - `from_mol(mol, **opts)`
    - `setup_calc(*, calc, **settings)`
    - `prep_ase(coords)`
    - `evaluate_term(coords, order, **opts)`
  - **class `MACEEnergyEvaluator`** (ASECalcEnergyEvaluator)
    - `handle_specialization(tag)`
    - `setup_calc(model='extra_large', model_type=None, device=None, model_dir=None, **settings)`
  - **class `UMAEnergyEvaluator`** (ASECalcEnergyEvaluator)
    - `handle_specialization(tag)`
    - `setup_calc(model='uma-s-1p2', device=None, task_name=None, model_dir=None, login=None, hf_token=None, predict_unit_options=None, **settings)`
  - **class `UPETEnergyEvaluator`** (ASECalcEnergyEvaluator)
    - `handle_specialization(tag)`
    - `setup_calc(model='pet-mad-s', device=None, version=None, login=None, hf_token=None, model_dir=None, **settings)`
  - **class `OrbModelEnergyEvaluator`** (ASECalcEnergyEvaluator)
    - `handle_specialization(tag)`
    - `setup_calc(model='omol', device=None, version=None, login=None, **model_opts)`
  - **class `HIPNNEnergyEvaluator`** (EnergyEvaluator)
    - `__init__(atoms, model_dir, property_key=dev.default, gradient_key=dev.default, charge=0, multiplicity=None, quiet=True, **defaults)`
    - `handle_specialization(tag)`
    - `from_mol(mol, *, model_dir, **opts)`
    - `autodiff(expr, coord_list, pad_dim, create_graph=False, retain_graph=False)`
    - `process_derivatives(expr, coords, order)`
    - `setup_model(model_dir, *, device=None, **opts)`
    - `print_model_properties()`
    - `process_results(base_shape, coords, outputs, order, property_key=None, gradient_key=None, property_shape=(), output_shape=None, **etc)`
    - `prep_eval(coords, order, **opts)`
    - `evaluate_term(coords, order, **opts)`
  - **class `XTBEnergyEvaluator`** (EnergyEvaluator)
    > Uses XTB to calculate
    - `__init__(atoms, method='GFN2-xTB', charge=0, quiet=True, **defaults)`
    - `handle_specialization(tag)`
    - `from_mol(mol, **opts)`
    - `get_single_point(coords)`
    - `evaluate_term(coords, order, **opts)`
  - **class `PySCFEnergyEvaluator`** (EnergyEvaluator)
    - `__init__(atoms, *, level_of_theory, basis_set, caller=None, charge=None, multiplicity=None, quiet=True, molecule_options=None, level_of_theory_options=None, disp=None, **defaults)`
    - `handle_specialization(tag)`
    - `caller_dispatch()`
    - `get_callers()`
    - `resolve_caller(caller, lot)`
    - `from_mol(mol, **opts)`
    - `get_molecule(coords)`
    - `get_energy_from_calc(calc)`
    - `get_gradient_from_calc(calc)`
    - `run_calculation(coords, **opts)`
    - `evaluate_term(coords, order, **opts)`
  - **class `EvaluationServerEnergyEvaluator`** (EnergyEvaluator)
    - `__init__(*, request_handler, **defaults)`
    - `setup_request(coords, order=None, tasks='energy', **opts)`
    - `process_response(response, state)`
    - `cleanup_state(response, state)`
    - `evaluate_term(coords, order, tasks='energy', **opts)`
    - `optimize(coords, method='server', **opts)`
  - **class `MLIPServerEnergyEvaluator`** (EvaluationServerEnergyEvaluator)
    - `__init__(atoms, *, container_path, energy_evaluator, session=None, port=None, temp_dir=None, launcher_options=None, bind_sources=True, container_env=None, container_mode=None, initialization_delay_time=1, charge=None, multiplicity=None, model_dir=dev.default, pass_tokens=True, **defaults)`
    - `from_mol(mol, **opts)`
    - `pick_port()`
    - `get_container_env_command()`
    - `get_launcher()`
    - `resolve_address()`
    - `launch_session()`
    - `cleanup_session()`
    - `get_io_files()`
    - `setup_request(coords, order=None, tasks='energy', **opts)`
    - `process_response(response, state)`
    - `cleanup_state(response, state)`
  - **class `PotentialFunctionEnergyEvaluator`** (EnergyEvaluator)
    - `__init__(potential_function, property_units=None, energy_units='Hartrees', distance_units='BohrRadius', **opts)`
    - `evaluate_term(coords, order, **opts)` — Evaluate the wrapped potential function, delegating to `PropertyFunctionEvaluator.evaluate_term`.
    - `use_internal_coordinate_handlers()` — Whether this evaluator should route coordinate handling through the internal-coordinate machinery,…
    - `evaluate(coords, order=0, logger=None, fd_handler=None, **opts)` — Evaluate the wrapped potential function's expansion, delegating to `PropertyFunctionEvaluator.evalu…
    - `get_property_function(prop_func, mol, **opts)` — Resolve the potential function to wrap; on this class, simply returns `prop_func` unchanged alongsi…
    - `from_mol(mol, property_function=None, potential_function=None, **opts)` — Build a `PotentialFunctionEnergyEvaluator` for a molecule, resolving the potential function from ei…
    - `bind_default(potential)` — Register a default potential function to be used the next time `from_mol` is called without an expl…
  - **class `PotentialExpansionEnergyEvaluator`** (PotentialFunctionEnergyEvaluator)
    - `__init__(expansion, center=None, ref=None, batched_orders=None, transforms=None, transformed_derivatives=False, **opts)`
    - `from_mol(mol, property_function=None, expansion=None, **opts)` — Build a `PotentialExpansionEnergyEvaluator` for a molecule, resolving the expansion from either `pr…
    - `get_property_function(expansion, mol, transforms=None, **ignored)` — Resolve the potential expansion to use: falls back to the molecule's own `potential_derivatives` if…
  - **class `DipoleEvaluator`** (PropertyEvaluator)
    - `get_evaluators()` — The built-in named dipole-evaluator constructors: an expansion-based evaluator, plus adapters for H…
    - `get_default_function_evaluator_type()` — The evaluator class used when a bare callable is supplied as a dipole-evaluator specification.
  - **class `DipoleFunctionDipoleEvaluator`** (DipoleEvaluator)
    - `__init__(potential_function, property_units=('ElementaryCharge', 'BohrRadius'), distance_units='BohrRadius', **opts)`
    - `embed_coords(coords, **kwargs)` — Embed coordinates into this evaluator's working representation, delegating to `PropertyFunctionEval…
    - `unembed_coords(coords, **kwargs)` — Convert coordinates back to plain Cartesians, delegating to `PropertyFunctionEvaluator.unembed_coor…
    - `unembed_derivs(base_coords, coords, derivs, **kwargs)` — Convert dipole derivative tensors back to plain Cartesian coordinates, delegating to `PropertyFunct…
    - `evaluate_term(coords, order, **opts)` — Evaluate the wrapped dipole function, delegating to `PropertyFunctionEvaluator.evaluate_term`.
    - `evaluate(coords, order=0, logger=None, **opts)` — Evaluate the wrapped dipole function's expansion, delegating to `PropertyFunctionEvaluator.evaluate…
    - `get_property_function(prop_func, mol, **opts)` — Resolve the dipole function to wrap; on this class, simply returns `prop_func` unchanged alongside…
    - `from_mol(mol, property_function=None, dipole_function=None, **opts)` — Build a `DipoleFunctionDipoleEvaluator` for a molecule, resolving the dipole function from either `…
    - `bind_default(potential)` — Register a default dipole function to be used the next time `from_mol` is called without an explici…
  - **class `DipoleExpansionDipoleEvaluator`** (DipoleFunctionDipoleEvaluator)
    - `__init__(expansion, center=None, ref=None, property_units=('ElementaryCharge', 'BohrRadius'), distance_units='BohrRadius', analytic_derivative_order=None, batched_orders=None, **opts)`
    - `expansion_from_mol_charges(mol, charges=None, origin=None)` — Build a first-order dipole expansion (a constant term and its Cartesian gradient) directly from a s…
    - `from_mol(mol, property_function=None, expansion=None, **opts)` — Build a `DipoleExpansionDipoleEvaluator` for a molecule, resolving the dipole expansion from either…
    - `get_property_function(expansion, mol, center=None, transforms=None, use_modes=True, **ignored)` — Resolve the dipole expansion to use: falls back to the molecule's own `dipole_derivatives` (or a ch…
  - **class `HIPNNDipoleEvaluator`** (DipoleEvaluator)
    - `__init__(atoms, model_dir, charge=0, multiplicity=None, quiet=True, **defaults)`
    - `from_mol(mol, *, model_dir, **opts)`
    - `evaluate_term(coords, order, **opts)`
  - **class `ChargeEvaluator`** (PropertyEvaluator)
    - `get_evaluators()` — The built-in named charge-evaluator constructors: adapters for RDKit and AIMNet2.
    - `get_default_function_evaluator_type()` — The evaluator class used when a bare callable is supplied as a charge-evaluator specification.
  - **class `ChargeFunctionChargeEvaluator`** (ChargeEvaluator)
    - `__init__(potential_function, property_units='ElementaryCharge', distance_units='BohrRadius', **opts)`
    - `evaluate_term(coords, order, **opts)` — Evaluate the wrapped charge function, delegating to `PropertyFunctionEvaluator.evaluate_term`.
    - `evaluate(coords, order=0, logger=None, **opts)` — Evaluate the wrapped charge function's expansion, delegating to `PropertyFunctionEvaluator.evaluate…
    - `from_mol(mol, property_function=None, **opts)` — Build a `ChargeFunctionChargeEvaluator` for a molecule, delegating to `PropertyFunctionEvaluator.in…
    - `bind_default(potential)` — Register a default charge function to be used the next time `from_mol` is called without an explici…
  - **class `RDKitChargeEvaluator`** (ChargeEvaluator)
    - `__init__(rdmol, model='gasteiger', **defaults)`
    - `from_mol(mol, charge=None, multiplicity=None, **opts)`
    - `evaluate_term(coords, order, model=None, **opts)`
  - **class `AIMNet2ChargeEvaluator`** (ChargeEvaluator)
    - `__init__(atoms, model='aimnet2', charge=0, multiplicity=None, quiet=True, **defaults)`
    - `from_mol(mol, **opts)`
    - `evaluate_term(coords, order, **opts)`
  - **class `ChargeEvaluatorDipoleEvaluator`** (DipoleEvaluator)
    - `__init__(charge_evaluator, **etc)`
    - `evaluate_term(coords, order, **opts)`
  - **class `AIMNet2DipoleEvaluator`** (ChargeEvaluatorDipoleEvaluator)
    - `from_mol(mol, **opts)`
  - **class `RDKitDipoleEvaluator`** (ChargeEvaluatorDipoleEvaluator)
    - `from_mol(mol, **opts)`
  - **class `DipolePolarizabilityEvaluator`** (PropertyEvaluator)
    - `get_evaluators()` — The built-in named dipole-polarizability-evaluator constructors: adapters for AIMNet2 and HIPNN.
    - `get_default_function_evaluator_type()` — The evaluator class used when a bare callable is supplied as a dipole-polarizability-evaluator spec…
    - `evaluate_polarizability_term(coords, coord_order, electrostatic_order, **opts)` — Abstract hook for evaluating the dipole-polarizability tensor's value (or specific coordinate/elect…
    - `evaluate_term(coords, order, **opts)` — Evaluate the polarizability's coordinate-derivative expansion at a given coordinate order, delegati…
  - **class `PolarizabilityFunctionDipolePolarizabilityEvaluator`** (DipolePolarizabilityEvaluator)
  - **class `FluctuatingChargeDipolePolarizabilityEvaluator`** (DipolePolarizabilityEvaluator)
    - `mu_factor(total_charge, X, J_inv)`
    - `dipole_factor_expansion(coords, total_charge, X, J_inv)`
    - `polarizability_factor(coords, J_inv)`
    - `evaluate_electrostatic_potential_derivatives(coords, coord_order, electrostatic_order, **opts)`
    - `evaluate_polarizability_term(coords, coord_order, electrostatic_order, *, charge, **opts)`
    - `expand_electrostatic_potential(total_charge, coords, potential_by_charges_by_coords)`
  - **class `AIMNet2DipolePolarizabilityEvaluator`** (FluctuatingChargeDipolePolarizabilityEvaluator)
    - `__init__(atoms, model='aimnet2', charge=0, multiplicity=None, quiet=True, **defaults)`
    - `from_mol(mol, **opts)`
    - `evaluate_polarizability_term(coords, coord_order, electrostatic_order, *, charge=None, **opts)`
    - `evaluate_electrostatic_potential_derivatives(coords, coord_order, electrostatic_order, **opts)`
  - **class `HIPNNDipolePolarizabilityEvaluator`** (FluctuatingChargeDipolePolarizabilityEvaluator)
    - `__init__(atoms, model_dir, **defaults)`
    - `from_mol(mol, *, model_dir, embedding=None, charge=None, multiplicity=None, **opts)`
    - `evaluate_polarizability_term(coords, coord_order, electrostatic_order, *, charge=None, **opts)`
    - `evaluate_electrostatic_potential_derivatives(coords, coord_order, electrostatic_order, **opts)`
  - **class `ReducedDimensionalPotentialHandler`**
    - `__init__(mol)`
    - `get_potential_params(spec, re, g, local_derivs, quartic_potential_cutoff=0.05, poly_expansion_order=2, **opts)` — Fit a simple analytic 1D potential form (a Morse potential, or a low-order polynomial in a Duschins…
    - `get_morse_potential(spec, *, w, wx, g, re)` — Build a Morse-potential `CoordinateFunction` for a given coordinate specification and fitted parame…
    - `get_poly_potential(spec, *, w_coeffs=None, coeffs=None, g, re)` — Build a polynomial `CoordinateFunction` for a given coordinate specification, either from raw Taylo…
    - `get_potential_types()` — The mapping from potential-form name to the constructor method that builds that form, used by the p…
    - `get_potentials_by_attributes()` — Attribute-based dispatch mapping used to infer which potential form to build from the keys present…
    - `potential_generator_dispatch()` — Lazily build (and cache) the `OptionsMethodDispatch` object used to resolve a parameter dict into t…
    - `get_g(which, return_tf=True, order=1)` — Compute the G-matrix (effective inverse-mass) element(s) for a given internal-coordinate specificat…
    - `get_anharmonic_parameters(which, evaluator=None, energy_expansion=None, params_handler=None, **opts)` — Fit 1D analytic potential parameters for each of a set of internal coordinates, by extracting each…
    - `get_1d_potentials(which, evaluator=None, energy_expansion=None, params_handler=None, potential_params=None, coordinate_potential_handler=None, **opts)` — Build 1D analytic potential-energy functions for a set of internal coordinates, using any explicitl…

### `Hamiltonian.py`
  - **class `MolecularHamiltonian`**
    > Provides a generic molecular Hamiltonian with support for expansions (for VPT),
    > construction of molecular models, and other wave function generators
    - `__init__(embedding, potential_manager=None, potential_derivatives=None, modes_manager=None, modes=None, dipole_manager=None, dipole_derivatives=None)`
    - `get_VPT_expansions(order=2, coordinate_transformation=None)` — Build the set of Taylor expansions (potential, kinetic/G-matrix, and either Coriolis + Watson or ps…
    - `prep_transformation(coordinate_transformation)` — Normalize a coordinate transformation into a `(forward, reverse)` pair of derivative-tensor lists,…
    - `get_embedding(masses=None, modes=True, mass_weight=True, dimensionless=False)` — Build a `ModeEmbedding` combining this Hamiltonian's coordinate embedding with (optionally) its nor…
    - `potential_expansion(order=None, *, dimensionless=False, embedding=None, exclude_gradient=False, **embedding_opts)` — Build (and optionally evaluate to a given order) a `ScalarExpansion` for the potential energy surfa…
    - `dipole_expansion(order=None, *, expansion=None, dimensionless=False, embedding=None, exclude_gradient=False, **embedding_opts)` — Build (and optionally evaluate to a given order) a `DipoleExpansion` for the dipole surface.
    - `get_potential_optmizing_transformation(order, *, dimensionless=False, embedding=None, exclude_gradient=True, **embedding_opts)` — Find the coordinate transformation that optimizes (diagonalizes/simplifies) the potential expansion…
    - `gmatrix_expansion(order=None, *, coords=None, dimensionless=False, embedding=None, **embedding_opts)` — Build (and optionally evaluate) the G-matrix (inverse kinetic-energy tensor) expansion, via `_get_k…
    - `get_kinetic_optmizing_transformation(order, *, dimensionless=False, embedding=None, **embedding_opts)` — Intended to iteratively construct the coordinate transformation that optimizes/decouples the kineti…
    - `pseudopotential_expansion(order=None, *, dimensionless=False, embedding=None, **embedding_opts)` — Build (and optionally evaluate) the pseudopotential expansion, via `_get_ke_expansion` with `Pseudo…
    - `coriolis_expansion(order=None, *, dimensionless=False, embedding=None, **embedding_opts)` — Build (and optionally evaluate) the Coriolis-coupling expansion, via `_get_ke_expansion` with `Cori…
    - `watson_expansion(order=None, *, dimensionless=False, embedding=None, **embedding_opts)` — Build (and optionally evaluate) the Watson (reciprocal-inertia) expansion, via `_get_ke_expansion`…
  - **class `ScalarOperatorManager`**
    - `__init__(manager_or_derivs)`
    - `derivatives()` — The wrapped operator's derivative tensors: delegates to `self.manager.derivatives` if `self.manager…
  - **class `PotentialManager`** (ScalarOperatorManager)
    - `__init__(manager_or_derivs, modes)`
    - `derivatives()` — The potential's canonicalized derivative tensors: fetches the raw derivatives (from `self.manager.d…
  - **class `ScalarExpansion`**
    > A helper class that can transform scalar derivatives from Cartesians (or Cartesian modes) to internals
    - `__init__(embedding, derivs)`
    - `canonicalize_derivs(derivs, shared=0)` — Re-express derivative tensors that are given with respect to mass-weighted Cartesians into the embe…
    - `check_mixed_expansion(derivs, shared=0)` — Determine whether a set of derivative tensors is "mixed" -- i.e.
    - `get_scalar_transformation(order, transformation=None)` — Fetch the internals-by-Cartesians and Cartesians-by-internals Jacobian expansions from the embeddin…
    - `get_terms(order=None, *, derivs=None, transformation=None, mixed_transformation=None, mixed_derivative_handling_mode='high', modes=None, shared=0)` — Evaluate the scalar function's Taylor-series terms in the embedding's target coordinate system (int…
  - **class `DipoleExpansion`** (ScalarExpansion)
    - `__init__(embedding, derivs)`
    - `get_terms(order=None, *, derivs=None, transformation=None, mixed_transformation=None, mixed_derivative_handling_mode='high', modes=None, shared=0)` — Evaluate the dipole's Taylor-series terms, temporarily separating the constant (reference) dipole v…
  - **class `GMatrixExpansion`**
    - `__init__(embedding)`
    - `get_terms(order, coords=None, transformation=None)` — Compute the G-matrix Taylor-expansion terms in the embedding's coordinates, by contracting the inte…
    - `reexpress_G(G_expansion, forward_derivs, reverse_derivs, order, *, GR_expansion=None, return_GR=False)` — Apply a coordinate transformation to the G-matrix
    - `reexpress(order, forward_derivs, reverse_derivs)` — Re-express this object's own G-matrix expansion (obtained via `get_terms`) through a new forward/re…
  - **class `PseudopotentialExpansion`**
    - `__init__(g_expansion)`
    - `lambda_expansion(G_expansion, G_inv, I0_expansion, I0_inv, order)` — Compute the expansion of `ln(det(I0)) - ln(det(G))`-type combination (the trace terms appearing in…
    - `get_U(G_expansion, I0_expansion)` — Compute the pseudopotential correction term `U` from the G-matrix and inertia-tensor (`I0`) expansi…
    - `get_terms(order, transformation=None)` — Evaluate the pseudopotential expansion terms up to `order`, either directly from the G-matrix and i…
    - `reexpress(order, forward_derivs, reverse_derivs)` — Apply a coordinate transformation to the G-matrix
  - **class `ZetaExpansion`**
    - `__init__(embedding)`
    - `get_terms(order, transformation=None)` — Compute the Coriolis zeta-constant expansion terms: builds the internals-by-Cartesians Jacobian (op…
  - **class `ReciprocalInertiaExpansion`**
    > Pulled from Watson
    - `__init__(embedding)`
    - `get_terms(order, transformation=None)` — Compute the expansion of the inverse inertia tensor `u = I0^-1` order by order, using the recursive…
  - **class `CoriolisRotationExpansion`**
    > Provides an expansion of the Coriolis rotation operator
    - `__init__(embedding)`
    - `get_terms(order, transformation=None)` — Compute the Coriolis rotation operator's expansion terms by combining the zeta-constant expansion (…

### `MolecularFunctionExpansion.py`
  - **class `JacobianKeys`** (enum.Enum)
    > Real access pattern: JacobianKeys.<MemberName> (this is an enum with 12 members, e.g. JacobianKeys.CartesiansByInternals == 'CartesiansByInternals'). Collapsed into a dict below purely for compactness -- do not index it as a dict in real code:
  - **class `DumbTensor`**
    > A wrapper to make tensor algebra suck less
    - `__init__(tensor)`
    - `shape()` — **LLM Docstring**
    - `dot(b, *args, **kwargs)` — Contract this tensor with `b` using `DumbTensor._dot`, unwrapping `b` first if it is itself a `Dumb…
    - `shift(*args, **kwargs)` — Apply `_shift` to this tensor's data and wrap the result in a new `DumbTensor`.
    - `transpose(*perm)` — Transpose the wrapped tensor according to `perm` and wrap the result in a new `DumbTensor`.
    - `contract_dim(targ_dim)` — Apply `_contract_dim` to this tensor's data and wrap the result in a new `DumbTensor`.
  - **class `MolecularFunctionExpander`**
    > Handles all necessary details to be able to
    > evaluate functions in molecular internal coordinates
    > and then transform back to Cartesians, and to do the
    > same with respect to normal dimensionless normal mode coordinates
    - `__init__(cartesian_coordinates, internal_coordinates, transform=None, masses=None, freqs=None, parallelizer=None, logger=None, checkpointer=None, numerical_jacobians=True, eckart_embed_derivatives=True, eckart_embed_planar_ref_tolerance=None, strip_dummies=False, strip_embedding=True, internal_fd_mesh_spacing=0.001, internal_fd_stencil=None, cartesian_fd_mesh_spacing=0.01, cartesian_fd_stencil=None, cartesian_analytic_deriv_order=0, internal_by_cartesian_order=3, cartesian_by_internal_order=4, jacobian_warning_threshold=10000.0)`
    - `undimensionalize(masses, freqs, modes)` — Removes units from normal modes
    - `get_int_jacobs(jacs)` — Gets the specified Internal->Cartesian Jacobians
    - `get_cart_jacobs(jacs)` — Gets the specified Cartesian->Internal Jacobians
    - `get_coordinate_transforms(internal_by_cartesian_order=None, cartesian_by_internal_order=None)` — Compute (and cache in `self._cached_transforms`) the full set of Jacobians relating Cartesian coord…
    - `cartesian_L_matrix()` — :return: the leading term of `get_cartesians_by_cartesian_modes(1)`
    - `get_cartesians_by_cartesian_modes(order=None)` — Fetch the Cartesians-by-Cartesian-normal-modes Jacobians up to the requested order, computing them…
    - `cartesian_L_inverse()` — :return: the leading term of `get_cartesian_modes_by_cartesians(1)`
    - `get_cartesian_modes_by_cartesians(order=None)` — Fetch the Cartesian-normal-modes-by-Cartesians Jacobians up to the requested order, computing them…
    - `internal_L_matrix()` — :return: the leading term of `get_internal_modes_by_internals(1)`
    - `get_internal_modes_by_internals(order=None, strip_embedding=True)` — Fetch the internal-normal-modes-by-internals Jacobians up to the requested order, optionally stripp…
    - `internal_L_inverse()` — :return: the leading term of `get_internals_by_internal_modes(1)`
    - `get_internals_by_internal_modes(order=None, strip_embedding=True)` — Fetch the internals-by-internal-normal-modes Jacobians up to the requested order, optionally stripp…
    - `cartesians_by_modes()` — All cached Cartesians-by-internal-modes Jacobians, computing the default set if not already cached.
    - `get_cartesians_by_modes(order=None)` — Fetch the Cartesians-by-internal-normal-modes Jacobians up to the requested order, computing them i…
    - `modes_by_cartesians()` — All cached internal-normal-modes-by-Cartesians Jacobians, computing the default set if not already…
    - `get_modes_by_cartesians(order=None, strip_embedding=True)` — Fetch the internal-normal-modes-by-Cartesians Jacobians up to the requested order.
    - `cartesians_by_internals()` — All cached Cartesians-by-internals Jacobians, computing the default set if not already cached.
    - `get_cartesians_by_internals(order=None, strip_embedding=False)` — Fetch the Cartesians-by-internals Jacobians up to the requested order, optionally stripping embeddi…
    - `internals_by_cartesians()` — All cached internals-by-Cartesians Jacobians, computing the default set if not already cached.
    - `get_internals_by_cartesians(order=None, strip_embedding=False)` — Fetch the internals-by-Cartesians Jacobians up to the requested order, optionally stripping embeddi…
    - `cartesian_modes_by_internal_modes()` — All cached Cartesian-normal-modes-by-internal-normal-modes Jacobians, computing the default set if…
    - `get_cartesian_modes_by_internal_modes(order=None)` — Fetch the Cartesian-normal-modes-by-internal-normal-modes Jacobians up to the requested order.
    - `internal_modes_by_cartesian_modes()` — All cached internal-normal-modes-by-Cartesian-normal-modes Jacobians, computing the default set if…
    - `get_internal_modes_by_cartesian_modes(order=None)` — Fetch the internal-normal-modes-by-Cartesian-normal-modes Jacobians up to the requested order.
    - `evaluate(function, deriv_order=2)` — Stub for evaluating `function` and its derivatives up to `deriv_order` in the transformed coordinat…

### `Molecule.py` — Provides a simple Molecule class that we can adapt as we need
  - **class `Molecule`** (AbstractMolecule)
    > General purpose 'Molecule' class where the 'Molecule' need not be a molecule at all
    - `__init__(atoms, coords, bonds=None, masses=None, name=None, internals=None, rdmol=None, dipole_surface=None, dipole_derivatives=None, potential_surface=None, potential_derivatives=None, normal_modes=None, source_file=None, guess_bonds=True, charge=None, formal_charges=None, spin=None, display_mode=None, display_settings=None, energy=None, energy_evaluator=None, dipole_evaluator=None, charge_evaluator=None, polarizability_evaluator=None, polarizability_derivatives=None, checkpoint_file=None, **metadata)`
    - `modify(atoms=dev.default, coords=dev.default, *, internals=dev.default, masses=dev.default, bonds=dev.default, guess_bonds=dev.default, energy=dev.default, energy_evaluator=dev.default, dipole_evaluator=dev.default, charge_evaluator=dev.default, polarizability_evaluator=dev.default, charge=dev.default, spin=dev.default, rdmol=dev.default, display_mode=dev.default, display_settings=dev.default, normal_modes=dev.default, dipole_surface=dev.default, potential_surface=dev.default, dipole_derivatives=dev.default, potential_derivatives=dev.default, polarizability_derivatives=dev.default, meta=dev.default, source_file=dev.default)` — Build a new `Molecule` that is a copy of this one with the given fields overridden, treating any ar…
    - `to_state(serializer=None)` — Serialize this molecule's essential data (atoms, masses, coordinates, bonds, internal-coordinate sp…
    - `from_state(data, serializer=None)` — Reconstruct a `Molecule` from a previously serialized state dict, by passing its entries directly a…
    - `cached_eval(key, generator, *, condition=None, args=(), kwargs=None)` — Evaluate (and cache, via this molecule's on-disk/in-memory checkpoint) a value under `key`, delegat…
    - `canonicalize_internals(spec, atoms, coords, bonds, relocalize=True, masses=None)` — Normalize the many accepted forms of an internal-coordinate specification (the strings `'auto'`/`'z…
    - `prep_internal_spec(spec, relocalize=True, masses=None)` — Canonicalize an internal-coordinate specification against this molecule's own atoms, coordinates, b…
    - `embedding()` — Property getter/setter for the molecule's `MolecularEmbedding`.
    - `embedding(e)` — Property getter/setter for the molecule's `MolecularEmbedding`.
    - `get_evaluator(embedding=None, normal_modes=dev.default)` — Build a `MolecularEvaluator` for this molecule's embedding (or an alternate one), using either the…
    - `evaluator()` — Property getter/setter for the `MolecularEvaluator` used for energy/derivative calculations.
    - `evaluator(e)` — Property getter/setter for the `MolecularEvaluator` used for energy/derivative calculations.
    - `coords()` — Property getter/setter for the Cartesian coordinates, delegating to `self.embedding.coords`.
    - `coords(coords)` — Property getter/setter for the Cartesian coordinates, delegating to `self.embedding.coords`.
    - `masses()` — Property getter/setter for the atomic masses.
    - `masses(masses)` — Property getter/setter for the atomic masses.
    - `internals()` — Getter for the raw (canonicalized) internal-coordinate specification, delegating to `self.embedding…
    - `charge()` — Property getter/setter for the molecule's net charge, stored in its metadata (getter defaults to `0…
    - `charge(c)` — Property getter/setter for the molecule's net charge, stored in its metadata (getter defaults to `0…
    - `spin()` — Property getter/setter for the molecule's spin, stored in its metadata.
    - `spin(c)` — Property getter/setter for the molecule's spin, stored in its metadata.
    - `charges()` — Property getter/setter for the molecule's per-atom partial charges, stored in its metadata.
    - `charges(c)` — Property getter/setter for the molecule's per-atom partial charges, stored in its metadata.
    - `formal_charges()` — Property getter/setter for the molecule's per-atom formal charges, stored in its metadata.
    - `formal_charges(c)` — Property getter/setter for the molecule's per-atom formal charges, stored in its metadata.
    - `get_charge_evaluator(evaluator=None, **opts)` — Resolve (and, if needed, instantiate from this molecule) a charge-evaluator object, defaulting to `…
    - `calculate_charges(evaluator=None, order=None, **opts)` — Compute partial-charge values (and, optionally, their derivatives) using the resolved charge evalua…
    - `internals(spec)` — Getter for the raw (canonicalized) internal-coordinate specification, delegating to `self.embedding…
    - `internal_coordinates()` — Getter for the internal coordinates at the molecule's current geometry, delegating to `self.embeddi…
    - `redundant_internal_transformation()` — Getter for the redundant-to-non-redundant internal-coordinate transformation, delegating to `self.e…
    - `get_coordinate_filer(allowed_coordinate_types=None, excluded_coordinate_types=None, allowed_ring_types=None, excluded_ring_types=None, allowed_group_types=None, excluded_group_types=None)` — Build a filter function (closing over the given allow/exclude criteria) that, given a dict of coord…
    - `get_bond_graph_internals(include_stretches=True, include_bends=True, include_dihedrals=True, include_fragments=True, pruning=None, fragment=None, base_internals=None, use_distance_matrix=True, concatenate=True)` — Build a set of internal coordinates (bond stretches, bends, dihedrals, and/or inter-fragment coordi…
    - `prune_internals(coords, method='b_matrix', check_rigidity=True)` — Reduce a set of internal coordinates down to a non-redundant, well-conditioned subset, defaulting t…
    - `get_labeled_internals(coordinate_filter=None, allowed_coordinate_types=None, excluded_coordinate_types=None, allowed_ring_types=None, excluded_ring_types=None, allowed_group_types=None, excluded_group_types=None, include_stretches=True, include_bends=True, include_dihedrals=True, include_fragments=True, coordinate_sorting=None, pruning=False)` — Build the internal coordinates from the bonding graph (via `get_bond_graph_internals`) and label ea…
    - `get_mode_labels(internals=None, modes=None, use_redundants=True, expansions=None, return_modes=False, **internals_opts)` — Assign human-readable labels (e.g.
    - `mode_embedding()` — The (cached) `ModeEmbedding` combining this molecule's coordinate embedding with its normal modes,…
    - `get_internals(coords=None, *, strip_embedding=True)` — Fetch internal coordinates, either the molecule's own cached ones or those for an alternate set of…
    - `get_cartesians_by_internals(order=None, coords=None, *, strip_embedding=False, **kw)` — Fetch the Cartesians-by-internals Jacobian expansion, via `self.embedding.get_cartesians_by_interna…
    - `get_internals_by_cartesians(order=None, *, coords=None, strip_embedding=False, **kw)` — Fetch the internals-by-Cartesians Jacobian expansion, via `self.embedding.get_internals_by_cartesia…
    - `get_cartesians_by_modes(order=None, **kw)` — Fetch the Cartesians-by-normal-modes Jacobian expansion, via `self.mode_embedding.get_cartesians_by…
    - `get_modes_by_cartesians(order=None, **kw)` — Fetch the normal-modes-by-Cartesians Jacobian expansion, via `self.mode_embedding.get_internals_by_…
    - `dipole_surface()` — :return:
    - `dipole_surface(val)` — Setter for the `DipoleSurfaceManager`, requiring the new value to already be one.
    - `dipole_derivatives()` — Property getter/setter for the dipole derivative tensors.
    - `dipole_derivatives(derivs)` — Property getter/setter for the dipole derivative tensors.
    - `get_cartesian_dipole_derivatives(order=None, evaluator=None, include_constant_term=False)` — Fetch the dipole derivatives in Cartesian coordinates, computing them via `calculate_dipole` (and c…
    - `get_internal_dipole_derivatives(order=None, reembed=True, strip_embedding=True)` — Fetch the dipole derivatives re-expressed in internal coordinates, by re-expanding the Cartesian di…
    - `get_cartesian_polarizability_derivatives(order=None, evaluator=None, include_constant_term=False)` — Fetch the dipole-polarizability derivatives in Cartesian coordinates, computing them via `calculate…
    - `get_hamiltonian(embedding=None, potential_derivatives=None, modes=None, dipole_derivatives=None, **etc)` — Build a `MolecularHamiltonian` for this molecule, defaulting to its own embedding, potential deriva…
    - `hamiltonian()` — Property getter/setter for the `MolecularHamiltonian`.
    - `hamiltonian(e)` — Property getter/setter for the `MolecularHamiltonian`.
    - `potential_surface()` — :return:
    - `potential_surface(val)` — Setter for the `PotentialSurfaceManager`, requiring the new value to already be one.
    - `potential_derivatives()` — Property getter/setter for the potential-energy derivative tensors.
    - `potential_derivatives(derivs)` — Property getter/setter for the potential-energy derivative tensors.
    - `get_cartesian_potential_derivatives(order=None, evaluator=None, use_cached=True)` — Fetch the potential-energy derivatives in Cartesian coordinates, computing them via `calculate_ener…
    - `get_internal_potential_derivatives(order=None, reembed=True, strip_embedding=True, zero_gradient=False)` — Fetch the potential-energy derivatives re-expressed in internal coordinates, by re-expanding the Ca…
    - `normal_modes()` — :return:
    - `normal_modes(val)` — Setter for the normal modes: wraps the given value in a `NormalModesManager` (via `NormalModesManag…
    - `get_normal_modes(masses=None, potential_derivatives=None, use_internals=None, project_transrot=True, **opts)` — Compute this molecule's normal modes (via `NormalModes.from_molecule`), optionally using alternate…
    - `get_reaction_path_modes(masses=None, potential_derivatives=None, **opts)` — Compute reaction-path-following normal modes for this molecule, via `ReactionPathModes.from_molecul…
    - `metadata()` — Property getter/setter for the molecule's metadata dict.
    - `metadata(val)` — Property getter/setter for the molecule's metadata dict.
    - `get_harmonic_spectrum(**opts)` — Build a harmonic IR spectrum for this molecule, via `HarmonicSpectrum.from_mol`.
    - `get_harmonic_raman_spectrum(**opts)` — Build a harmonic Raman spectrum for this molecule, via `HarmonicSpectrum.raman_from_mol`.
    - `num_atoms()` — The number of atoms in the molecule.
    - `atom_positions()` — A mapping from element symbol to the list of atom indices having that symbol.
    - `dummy_positions()` — The indices of any dummy (`'X'`) atoms in the molecule.
    - `atoms()` — The element symbols of the molecule's atoms.
    - `atomic_masses()` — The atomic masses in atomic units (electron masses), via `_atomic_masses`.
    - `bonds()` — Property getter/setter for the molecule's bonds.
    - `bonds(b)` — Property getter/setter for the molecule's bonds.
    - `break_bonds(bonds, use_rdkit=False, **rdopts)` — Build a copy of this molecule with the specified bonds removed, either by delegating to the attache…
    - `formula()` — The molecule's chemical formula, via `self.prop('chemical_formula')`.
    - `multiconfig()` — Whether this molecule holds multiple geometry configurations at once, delegating to `self.coords.mu…
    - `name()` — The molecule's name, falling back to `"Unnamed"` if none was set.
    - `source_file()` — Setter for the source file: if given a plain path string, infers the source `mode` from its extensi…
    - `source_file(src)` — Setter for the source file: if given a plain path string, infers the source `mode` from its extensi…
    - `source_mode()` — The inferred/stored source-file mode (e.g.
    - `shape()` — The shape of the molecule's Cartesian coordinates.
    - `copy()` — Make a copy of this molecule by calling `modify()` with no overrides.
    - `take_submolecule(pos)` — Build a new molecule containing only the atoms at the given positions, remapping bonds to the new (…
    - `prop(name, *args, **kwargs)` — Compute a named derived molecular property by dispatching to the corresponding function on `Molecul…
    - `get_guessed_bonds(mode=None, **opts)` — Guess the bonding arrangement for this molecule, either via RDKit (from the Cartesian geometry) or…
    - `edge_graph()` — The (cached) `EdgeGraph` representation of the molecule's bonding structure, built lazily via `Mole…
    - `find_path(atom1, atom2)` — Find a path between two atoms through the bonding graph.
    - `find_substructure(pattern)` — Find matches of a substructure pattern within the molecule, delegating to the attached RDKit molecu…
    - `apply_smarts(pattern)` — Apply a SMARTS reaction/transformation pattern to the molecule via RDKit, returning each resulting…
    - `neighborhood(loc, size=1)` — Find the atoms within a given graph-distance of a location in the bonding graph.
    - `remove_hydrogens(positions=None, max=None, *, hydrogen_types=None)` — Build a copy of this molecule with hydrogen-type atoms (`H`/`D`/`T` by default) removed, either all…
    - `fragment_embedding(fragment_indices, ref=None, return_axes=False, view_inds=(1, 2), use_moments=False)` — Compute a local coordinate frame (origin, offset vector, and an up-vector or full axis set) anchore…
    - `attach_functional_group(target_fragment, atoms, new_coords, bonds='recompute', ref=None, masses=None, distance='auto', angle=0, dihedral=0, embedding='auto', bond_order=None, use_absolue_posititions=False, group_site=None)` — Build a copy of this molecule with a new group of atoms (`atoms`/`new_coords`) attached at `target_…
    - `find_heavy_atom_backbone(root=None)` — Find the longest chain of atoms in the bonding graph (the heavy-atom backbone), via `edge_graph.fin…
    - `find_backbone_segments(root=None, initial_backbone=None)` — Split the bonding graph into backbone-connected segments, via `edge_graph.segment_by_chains`.
    - `get_backbone_zmatrix(root=None, segments=None, return_remainder=False, return_segments=False, required_coordinates=None, isolated_coordinates=None, root_coordinates=None, initial_backbone=None, validate=True)` — Build a Z-matrix for a (typically single-fragment) molecule by first segmenting its bonding graph i…
    - `get_canonical_zmatrix(ordering=None, validate=True)` — Build a canonical Z-matrix ordering for the molecule from a canonical fragmentation of the bonding…
    - `get_bond_zmatrix(fragments=None, segments=None, root=None, required_coordinates=None, isolated_coordinates=None, root_coordinates=None, attachment_points=None, check_attachment_points=True, validate=True, for_fragment=None, fragment_ordering=None, connect_fragments=True, initial_backbone=None)` — Build a full Z-matrix for the molecule from its bonding graph, handling the single-fragment case vi…
    - `fragment_indices()` — The (cached) grouping of atom indices into connected molecular fragments, computed lazily via `Mole…
    - `fragments()` — The molecule split into its connected fragments (as separate `Molecule` objects), via `MolecularPro…
    - `mass_weighted_coords()` — :return:
    - `center_of_mass()` — :return:
    - `inertia_tensor()` — :return:
    - `inertial_eigensystem()` — :return:
    - `moments_of_inertia()` — :return:
    - `inertial_axes()` — :return:
    - `translation_rotation_modes()` — :return:
    - `get_translation_rotation_projector(mass_weighted=False)` — Build the projector matrix that removes translational and rotational displacement components from a…
    - `get_translation_rotation_invariant_transformation(mass_weighted=False, strip_embedding=True)` — Build the transformation (and its inverse) that projects out the translational and rotational degre…
    - `energy()` — The molecule's total energy: loaded from its source file if possible, else computed via `calculate_…
    - `get_energy_evaluator(evaluator=None, **opts)` — Resolve (and, if needed, instantiate from this molecule) an energy-evaluator object, defaulting to…
    - `get_energy_function(evaluator=None, *, order=None, **opts)` — Build a standalone callable that evaluates this molecule's energy (and derivatives) at arbitrary co…
    - `calculate_energy(coords=None, *, evaluator=None, order=None, **opts)` — Compute the energy (and, optionally, its derivatives) at the given (or this molecule's own) coordin…
    - `partial_force_field(coords=None, modes=None, *, evaluator=None, order=4, mesh_spacing=1, analytic_derivative_order=None, **opts)` — Compute a partial (mode-selected) force-field expansion of the potential, evaluated in the given no…
    - `optimize(evaluator=None, *, method=None, tol=None, max_iterations=None, logger=None, reembed=True, **opts)` — Geometry-optimize the molecule using the resolved energy evaluator, splitting `opts` into optimizer…
    - `relaxed_scan(scan_values, scan_coordinates, evaluator=None, *, method=None, tol=None, max_iterations=None, logger=None, reembed=False, **opts)` — Perform a relaxed (constrained) potential-energy scan over the given coordinates/values using the r…
    - `get_dipole_evaluator(evaluator=None, **opts)` — Resolve (and, if needed, instantiate from this molecule) a dipole-evaluator object, defaulting to `…
    - `get_dipole_function(evaluator=None, *, order=None, **opts)` — Build a standalone callable that evaluates this molecule's dipole (and derivatives) at arbitrary co…
    - `calculate_dipole(evaluator=None, order=None, **opts)` — Compute the dipole moment (and, optionally, its derivatives) at this molecule's current coordinates…
    - `get_polarizability_evaluator(evaluator=None, **opts)` — Resolve (and, if needed, instantiate from this molecule) a dipole-polarizability-evaluator object,…
    - `get_dipole_polarizability_function(evaluator=None, *, order=None, **opts)` — Build a standalone callable that evaluates this molecule's dipole polarizability (and derivatives)…
    - `calculate_dipole_polarizability(evaluator=None, order=None, **opts)` — Compute the dipole polarizability (and, optionally, its derivatives) at this molecule's current coo…
    - `polarizability_derivatives()` — Property getter/setter for the dipole-polarizability derivative tensors, delegating to `self.dipole…
    - `polarizability_derivatives(derivs)` — Property getter/setter for the dipole-polarizability derivative tensors, delegating to `self.dipole…
    - `get_reduced_potential_generator()` — Build a `ReducedDimensionalPotentialHandler` for generating reduced-dimensionality potential slices…
    - `get_1d_potentials(spec, evaluator=None, energy_expansion=None, potential_params=None, **opts)` — Generate 1D potential-energy slices along the specified coordinate(s), via a `ReducedDimensionalPot…
    - `evaluate(func, use_internals=None, order=None, strip_embedding=False)` — Evaluate an arbitrary function of the molecule's coordinates (and its derivatives) via the molecule…
    - `evaluate_at(func, coords, use_internals=None, order=None, strip_embedding=False)` — Evaluate an arbitrary function of the molecule's coordinates (and its derivatives) at a specific se…
    - `get_displaced_coordinates(displacements, which=None, sel=None, axes=None, use_internals=False, coordinate_expansion=None, strip_embedding=False, shift=True)` — Build displaced copies of the molecule's coordinates along specified atoms/axes, via the molecule's…
    - `get_scan_coordinates(domains, internals=False, modes=None, order=None, which=None, sel=None, axes=None, shift=True, coordinate_expansion=None, strip_embedding=False, return_displacements=False)` — Build a grid of displaced coordinates spanning the given coordinate domains, optionally re-expresse…
    - `get_nearest_displacement_atoms(points, sel=None, axes=None, weighting_function=None, return_distances=False)` — Find the atoms nearest to a set of query points (optionally restricted to a selection/axes, with a…
    - `get_nearest_displacement_coordinates(points, sel=None, axes=None, weighting_function=None, modes_nearest=False, return_distances=False)` — Find the displaced coordinates nearest to a set of query points, optionally in normal-mode space, v…
    - `get_nearest_scan_coordinates(domains, sel=None, axes=None)` — Find the scan-grid coordinates nearest to a set of query domains, via the molecule's `MolecularEval…
    - `plot_molecule_function(function, *, axes, sel=None, embed=False, modes_nearest=False, domain=None, domain_padding=1, plot_points=500, weighting_function=None, mask_function=None, mask_value=0, plot_atoms=False, atom_colors=None, atom_radii=None, plotter=None, epilog=None, **plot_options)` — Plot an arbitrary scalar function of the molecule's geometry over a 1D or 2D grid of displaced coor…
    - `get_model(potential_specs, dipole=None)` — Build an analytic `MolecularModel` (a symbolic potential/dipole surface expressed in terms of this…
    - `get_point_group(coords=None, masses=None, *, sel=None, verbose=False, return_identifier=False, **tols)` — Identify the molecular point group at the given (or this molecule's own) geometry, via `McUtils.Sym…
    - `point_group()` — Property getter/setter for the molecule's (cached) point group.
    - `point_group(point_group)` — Property getter/setter for the molecule's (cached) point group.
    - `get_point_group_embedded_coordinates(pg=None, sel=None, return_point_group=False, return_identifier=False, **tols)` — Compute this molecule's coordinates re-embedded into the standard reference frame of its molecular…
    - `symmetrize(pg=None, return_identifier=False, tol=0.1, sel=None, return_coordinates=None, return_point_group=False, **tols)` — Symmetrize the molecule's geometry to conform exactly to a (identified or given) point group, addin…
    - `get_symmetrized_internals(point_group=None, *, internals=None, extra_internals=None, masses=None, return_expansions=False, atom_selection=None, as_characters=True, normalize=None, drop_empty_modes=None, perms=None, return_base_expansion=False, return_point_group=False, reduce_redundant_coordinates=None, ops=None, permutation_tol=0.01, **opts)` — Build symmetry-adapted linear combinations of internal coordinates for the molecule under a (identi…
    - `get_surface(radius_type='VanDerWaalsRadius', *, surface_type=None, radius_units='Angstroms', samples=100, radius_scaling=1, **etc)` — Build a surface representation of the molecule (by default, a union of per-atom spheres) using per-…
    - `get_surface_mesh(radius_type='VanDerWaalsRadius', *, surface_type=None, radius_units='Angstroms', samples=50, expansion=0.01, mesh_options=None, **etc)` — Build a triangulated mesh of the molecule's surface, via `get_surface` followed by `generate_mesh`.
    - `setup_AIMD(potential_function=None, timestep=0.5, seed=None, total_energy=None, total_energy_scaling=None, trajectories=1, sampled_modes=None, initial_energies=None, initial_displacements=None, initial_mode_directions=None, displaced_coords=None, track_kinetic_energy=False, track_velocities=False, modes=None, **etc)` — Set up an `AIMDSimulator` (ab initio / classical molecular dynamics) for this molecule, either prop…
    - `setup_VPT(*, states=2, order=2, use_internals=None, potential_derivatives=None, energy_evaluator=None, dipole_derivatives=None, dipole_evaluator=None, runner='matrix', use_reaction_path=False, modes=None, projected_modes=None, mode_transformation=None, potential_terms=None, dipole_terms=None, **opts)` — Set up a vibrational perturbation theory (VPT) calculation for this molecule, resolving the potenti…
    - `setup_job(job_type, *args, **kwargs)` — Set up an external-program computational job (e.g.
    - `get_gmatrix(masses=None, coords=None, use_internals=None, power=None, **internals_opts)` — Compute the G-matrix (inverse effective-mass metric tensor) for this molecule, either the trivial d…
    - `g_matrix()` — Returns the molecular g-matrix for the system
    - `coriolis_constants()` — Returns the molecular g-matrix for the system
    - `principle_axis_frame(sel=None, inverse=False)` — Gets the principle axis frame(s) for the molecule
    - `principle_axis_data()` — Gets the principle axis embedded coords and embedding parameters for the molecule
    - `permute_atoms(perm)` — Build a copy of this molecule with its atoms reordered according to a permutation, remapping bonds…
    - `apply_affine_transformation(transformation, load_properties=False, embed_properties=True)` — Apply an affine (rotation/translation) transformation to the molecule's coordinates, optionally pro…
    - `apply_rotation(rotation_matrix, shift_com=None, load_properties=None, embed_properties=True)` — Apply a rotation (optionally shifted to rotate about the center of mass) to the molecule, via `appl…
    - `eckart_frame(mol, sel=None, inverse=False, planar_ref_tolerance=None, proper_rotation=False, reset_com=True)` — Gets the Eckart frame(s) for the molecule
    - `embed_coords(crds, sel=None, in_paf=False, planar_ref_tolerance=None, proper_rotation=False)` — Embeds coords in the Eckart frame using `self` as a reference
    - `get_embedding_data(crds, sel=None, in_paf=False, planar_ref_tolerance=None, proper_rotation=False)` — Gets the necessary data to embed crds in the Eckart frame using `self` as a reference
    - `get_embedded_molecule(ref=None, sel=None, planar_ref_tolerance=None, proper_rotation=False, embed_properties=True, load_properties=None, reset_com=True)` — Returns a Molecule embedded in an Eckart frame if ref is not None, otherwise returns
    - `get_rmsd(other, sel=None, embed=True, embedding_sel=None, mass_weighted=False)` — Compute the (optionally mass-weighted) root-mean-square displacement between this molecule's geomet…
    - `align_molecule(other, reindex_bonds=True, permute_atoms=True, align_structures=True, sel=None, embed_properties=True, load_properties=False)` — Aligns `other` with `self` by first finding the reindexing of the bonds of `other` that
    - `rdmol()` — The (cached) attached RDKit molecule representation, built lazily via `RDMolecule.from_mol` the fir…
    - `to_ase(**kwargs)` — Convert this molecule to an ASE (Atomic Simulation Environment) molecule object, via `ASEMolecule.f…
    - `from_ase(ase_mol, **kwargs)` — Build a `Molecule` from an ASE molecule object.
    - `from_zmat(zmat, internals=None, axes=None, origin=None, **opts)` — Build a `Molecule` from a Z-matrix specification (either a Z-matrix string or an explicit `(atoms,…
    - `from_openbabel(mol, **opts)` — :param mol:
    - `get_obmol(**opts)` — Convert this molecule to an OpenBabel molecule object, via `OBMolecule.from_mol`.
    - `from_rdmol(rdmol, **opts)` — Build a `Molecule` from an RDKit molecule (or a raw RDKit `Mol`/owning-mol object, which is first w…
    - `from_name(name, **opts)` — Build a `Molecule` by looking up a compound name, via `from_string` with format `'name'`.
    - `get_atom_strings()` — The (cached) set of up-to-2-character atomic element symbols known to `AtomData`, used for heuristi…
    - `get_string_format_dispatchers()` — The mapping from string-format key to the constructor method that parses that format, used by `from…
    - `from_string(string, fmt=None, allow_names=False, format_options=None, **opts)` — Build a `Molecule` from a raw string in any supported structural format, inferring the format autom…
    - `get_file_format_dispatchers()` — The mapping from file-format key (typically a file extension) to the constructor method that parses…
    - `from_file(file, mode=None, format_options=None, use_ob_fallback=False, **opts)` — In general we'll delegate to pybel except for like Fchk and Log files
    - `get_string_export_dispatchers()` — The mapping from string-export format key to the exporter method that produces that format, used by…
    - `to_string(fmt, **opts)` — Export this molecule to a string in the given format, dispatching to an in-memory string exporter i…
    - `get_file_export_dispatchers()` — The mapping from file-export format key to the exporter method that writes that format to disk, use…
    - `to_file(file, mode=None, use_ob_fallback=False, **opts)` — :param file:
    - `construct(spec, fmt=None, **opts)` — Universal `Molecule` constructor: builds a molecule from essentially any reasonable input (an exist…
    - `plot(*geometries, figure=None, return_objects=False, bonds=None, bond_radius=None, atom_radius_scaling=None, atom_style=None, atom_radii=None, atom_text=None, display_atom_numbers=False, radius_type=None, bond_style=None, reconcile_bonds=True, capped_bonds=None, reflectiveness=None, vector_style=None, highlight_atoms=None, highlight_bonds=None, highlight_rings=None, highlight_styles=None, comparison_styles=None, animation_frame_styles=None, mode_vectors=None, mode_vector_origins=None, mode_vector_origin_mode='set', mode_vector_display_cutoff=0.01, principle_axes=None, principle_axes_origin=None, principle_axes_origin_mode='set', principle_axes_style=None, dipole=None, dipole_origin=None, dipole_origin_mode='set', render_multiple_bonds=None, render_fractional_bonds=None, fractional_bond_offset=None, bond_center_radius_offset=None, draw_coords=None, draw_coords_style=None, up_vector=None, multiple_bond_spacing=None, mode=None, backend=None, include_save_buttons=None, objects=False, graphics_class=None, cylinder_class=None, cylinder_options=None, sphere_class=None, sphere_options=None, arrow_class=None, arrow_options=None, line_class=None, line_options=None, disk_class=None, disk_options=None, animate=None, recording_options=None, animation_options=None, jsmol_load_script=None, include_jsmol_script_interface=None, dynamic_loading=None, units='Angstroms', label_style=None, theme='default', theme_function=None, plot_range_padding='auto', annotation_function=None, **plot_ops)` — Dispatches to the appropriate `MoleculePlotter` for the resolved backend/mode.
    - `get_animation_geoms(which, extent=0.35, steps=8, strip_embedding=True, units=None, coordinate_expansion=None)` — Build a back-and-forth looping sequence of displaced geometries for animating a single coordinate (…
    - `animate_coordinate(which, extent=0.5, steps=3, return_objects=False, strip_embedding=True, units='Angstroms', backend=None, mode=None, jsmol_load_script=None, coordinate_expansion=None, **plot_opts)` — Build an animation of a displaced coordinate and return it as a plottable/displayable object, eithe…
    - `animate_mode(which, extent=0.5, steps=3, modes=None, coordinate_expansion=None, order=None, normalize=True, mass_weight=False, mass_scale=True, frequency_scale=False, **opts)` — Build an animation of a normal mode's displacement, converting the mode into a coordinate-expansion…
    - `format_structs(geoms, format='xyz')` — Format a batch of geometries as a concatenated multi-frame string in the given format.
    - `format_animation_file(which, format='xyz', extent=0.35, steps=8, strip_embedding=True, units='Angstroms', coordinate_expansion=None)` — Build a formatted animation string/block for a displaced coordinate, either as a single JSMol vibra…
    - `to_widget()` — Build a displayable widget for this molecule, either a JSMol applet or an X3D scene, depending on `…
  - **class `MolecoolException`** (Exception)

### `MoleculeInterface.py` — Defines the fundamental interface we expect all molecules to support
  - **class `PotentialSurfaceType`** (typing.Protocol)
    - `surface()` — Protocol stub for a property that provides access to the underlying potential energy surface data.
    - `derivatives()` — Protocol stub for a property that provides the derivatives of the potential surface (e.g.
    - `load()` — Protocol stub for loading the potential surface data.
    - `update(val)` — Protocol stub for updating the stored potential surface with a new value.
    - `insert_atoms(atoms, coords, where)` — Protocol stub describing how the potential surface should be updated when atoms are inserted into t…
    - `delete_atoms(where)` — Protocol stub describing how the potential surface should be updated when atoms are removed from th…
    - `apply_transformation(frame)` — Protocol stub describing how the potential surface should respond to a coordinate frame transformat…
  - **class `DipoleSurfaceType`** (typing.Protocol)
    - `surface()` — Protocol stub for a property that provides access to the underlying dipole surface data.
    - `derivatives()` — Protocol stub for a property that provides the derivatives of the dipole surface.
    - `load()` — Protocol stub for loading the dipole surface data.
    - `update(val)` — Protocol stub for updating the stored dipole surface with a new value.
    - `insert_atoms(atoms, coords, where)` — Protocol stub describing how the dipole surface should be updated when atoms are inserted into the…
    - `delete_atoms(where)` — Protocol stub describing how the dipole surface should be updated when atoms are removed from the u…
    - `apply_transformation(frame)` — Protocol stub describing how the dipole surface should respond to a coordinate frame transformation…
  - **class `NormalModesType`** (typing.Protocol)
    - `modes()` — Protocol stub for a property that provides access to the underlying normal-mode data.
    - `load()` — Protocol stub for loading the normal-mode data.
    - `update(val)` — Protocol stub for updating the stored normal modes with a new value.
    - `insert_atoms(atoms, coords, where)` — Protocol stub describing how the normal modes should be updated when atoms are inserted into the un…
    - `delete_atoms(where)` — Protocol stub describing how the normal modes should be updated when atoms are removed from the und…
    - `apply_transformation(frame)` — Protocol stub describing how the normal modes should respond to a coordinate frame transformation b…
  - **class `AbstractMolecule`**
    > A molecule base class to provide type constraints
    - `name()` — Provides the name of the molecule
    - `metadata()` — Provides the metadta the molecule
    - `atoms()` — Provides the atom names for the molecule
    - `masses()` — Provides the masses for the molecule
    - `atomic_masses()` — Provides the atomic masses for the molecule
    - `coords()` — Provides the atomic coordinates for the molecule
    - `coords(new)` — Sets the atomic coordinates for the molecule
    - `internal_coordinates()` — Provides the internal coordinates for the molecule
    - `center_of_mass()` — Provides the center of mass for the molecule
    - `inertial_axes()` — Provides the principal axes for the molecule
    - `bonds()` — Provides the bonds for the molecule
    - `potential_surface()` — Provides the potential surface for the molecule
    - `dipole_surface()` — Provides the dipole surface for the molecule
    - `normal_modes()` — Provides the normal modes for the molecule
    - `source_file()` — Provides the source file for the molecule
    - `copy()` — Copies the molecule
    - `take_submolecule(pos)` — Takes a submolecule

### `Properties.py` — A collection of methods used in computing molecule properties
  - **class `MolecularPropertyError`** (Exception)
    > General error class for MolecularProperties
  - **class `StructuralProperties`**
    > The set of molecular properties that depend on its coordinates/configuration.
    > Slowly trying to move code out of this and into numputils/Hamiltonian/Evaluator
    - `get_prop_mass_weighted_coords(coords, masses)` — Gets the mass-weighted coordinates for the system
    - `get_prop_center_of_mass(coords, masses)` — Gets the center of mass for the coordinates
    - `get_prop_inertia_tensors(coords, masses)` — Computes the moment of intertia tensors for the walkers with coordinates coords (assumes all have t…
    - `get_prop_inertial_frame_derivatives(crds, mass, sel=None)` — Compute the first and second derivatives of the (mass-weighted) inertia tensor with respect to mass…
    - `get_prop_moments_of_inertia(coords, masses)` — Computes the moment of inertia tensor for the walkers with coordinates coords (assumes all have the…
    - `get_prop_principle_axis_rotation(coords, masses, sel=None, inverse=False, shift_sel=True)` — Generates the principle axis transformation for a set of coordinates and positions
    - `get_principle_axis_embedded_coords(coords, masses, sel=None)` — Returns coordinate embedded in the principle axis frame
    - `get_prop_principle_axis_data(coords, masses)` — Generates the principle axis transformation for a set of coordinates and positions
    - `get_prop_translation_rotation_eigenvectors(coords, masses)` — Returns the eigenvectors corresponding to translations and rotations
    - `get_eckart_rotations(masses, ref, coords, sel=None, in_paf=False, planar_ref_tolerance=None, proper_rotation=False)` — Generates the Eckart rotation that will align ref and coords, assuming initially that `ref` and `co…
    - `get_eckart_embedding_data(masses, ref, coords, sel=None, in_paf=False, planar_ref_tolerance=None, proper_rotation=False)` — Embeds a set of coordinates in the reference frame
    - `get_prop_eckart_transformation(masses, ref, coords, sel=None, inverse=False, reset_com=False, in_paf=False, planar_ref_tolerance=None, proper_rotation=False)` — Computes Eckart transformations for a set of coordinates
    - `get_eckart_embedded_coords(masses, ref, coords, reset_com=True, in_paf=False, sel=None, planar_ref_tolerance=None, proper_rotation=False)` — Embeds a set of coordinates in the reference frame
    - `get_prop_g_matrix(masses, coords, internal_coords)` — Gets the molecular g-matrix
    - `get_prop_coriolis_constants(carts, modes, masses)` — Compute the Coriolis zeta constants for a set of normal modes, by projecting the mode displacement…
  - **class `BondingProperties`**
    > The set of properties that depend only on bonding
    > and that kind of format
    - `get_prop_adjacency_matrix(atoms, bonds)` — Returns the adjacency matrix for the molecule
    - `get_prop_connectivity(atoms, bonds)` — Returns the adjacency matrix for the molecule
    - `get_prop_fragments(atoms, bonds)` — Returns the fragments for the molecule
    - `get_prop_zmat_ordering(atoms, bonds)` — Gets a guessed Z-matrix ordering for the molecule with connectivity defined by bonds based on the f…
    - `get_prop_guessed_bonds(mol, tol=1.05, guess_type=True, covalent_radius_scaling=1.1)` — Guesses the bonds for the molecule by finding the ones that are less than some percentage of a sing…
  - **class `MolecularProperties`**
    > An object whose sole purpose in life is to get molecular properties
    > A property should be implemented in two parts:
    >     1) a classmethod called get_prop_<prop name> that takes raw inputs and uses them to compute a property
    >     2) a classmethod called <prop name> that extracts the property from a passed molecule
    > *(truncated — see stub for full docstring)*
    - `mass_weighted_coords(mol)` — Computes the moments of inertia
    - `g_matrix(mol)` — :param mol:
    - `center_of_mass(mol)` — Computes the moments of inertia
    - `inertia_tensor(mol)` — Computes the inertia tensors for the stored geometries
    - `moments_of_inertia(mol)` — Computes the moments of inertia
    - `principle_axis_data(mol, sel=None)` — Generates the center of masses and inertial axes
    - `principle_axis_transformation(mol, sel=None, inverse=False)` — Generates the principle axis transformation for a Molecule
    - `eckart_embedding_data(mol, coords, sel=None, in_paf=False, planar_ref_tolerance=None, proper_rotation=False)` — :param mol:
    - `eckart_transformation(mol, ref_mol, sel=None, inverse=False, planar_ref_tolerance=None, reset_com=True, proper_rotation=False)` — :param mol: molecules to get Eckart embeddings for
    - `eckart_embedded_coords(mol, coords, sel=None, in_paf=False, reset_com=True, planar_ref_tolerance=None, proper_rotation=False)` — :param mol:
    - `coriolis_constants(mol)` — Compute the Coriolis zeta constants for a molecule's own normal modes, using its Cartesian coordina…
    - `translation_rotation_eigenvectors(mol, sel=None)` — :param mol: molecules to get eigenvectors for
    - `fragments(mol)` — :param mol:
    - `fragment_indices(mol)` — :param mol:
    - `edge_graph(mol)` — :param mol:
    - `guessed_bonds(mol, tol=1.05, guess_type=True)` — Guesses the bonds for the molecule by finding the ones that are less than some percentage of a sing…
    - `get_prop_chemical_formula(atoms)` — :param atoms:
    - `chemical_formula(mol)` — :param mol:
  - **class `StringFormatHandler`**
    > A base class to handle converting to/from string formats
    > mostly just here to implement SDF so OpenBabel can read off that
    - `__init__(mol)`
    - `get_string(atoms, coords, bonds, meta)` — Converts the molecular info to string format
    - `convert()` — Convert the stored molecule to its string representation by extracting its atoms, coordinates, bond…
    - `parse_string(str)` — :param str:
    - `parse(string)` — Parse a string representation back into a `Molecule` object, using the (subclass-defined) `parse_st…
  - **class `SDFFormatHandler`** (StringFormatHandler)
    - `__init__(mol)`
    - `convert_header(comment=None)` — Build the 3-line SDF header block: the molecule's name, the generating program name, and a comment…
    - `convert_counts_line(atoms, bonds)` — Build the SDF "counts" line giving the number of atoms and bonds, plus the fixed structural-data su…
    - `convert_coordinate_block(atoms, coords)` — Build the SDF atom block: one fixed-width line per atom giving its Cartesian coordinates and elemen…
    - `convert_bond_block(bonds)` — Build the SDF bond block: one fixed-width line per bond giving the 1-indexed atom pair and bond ord…
    - `convert_metadata(meta)` — Build the SDF metadata block, formatting each key/value pair as an SDF data-item tag (`>  <key>` fo…
    - `get_single_structure_string(atoms, coords, bonds, meta)` — Assemble the full SDF record for a single structure by combining the header, counts line, atom bloc…
    - `get_string(atoms, coords, bonds, meta)` — Converts the molecular info to string format
    - `parse_string(str)` — :param str:
  - **class `PropertyManager`**
    > A utility base class so to make it easier to have a unified way to
    > handled derived properties
    - `__init__(mol)`
    - `set_molecule(mol)` — Rebind this manager to a different molecule.
    - `from_data(mol, data)` — Abstract constructor for building a property manager directly from raw data.
    - `load()` — Loads in the values
    - `update(val)` — Updates the held values
    - `apply_transformation(transf)` — Applies a transformation to the held values
    - `insert_atoms(atoms, coords, where)` — Handles the insertion of new atoms into the structure
    - `delete_atoms(where)` — Handles the deletion from the structure
    - `copy()` — Make a shallow copy of this manager.
  - **class `OpenBabelMolManager`** (PropertyManager)
    - `__init__(mol, obmol=None)`
    - `load()` — Lazily build (and cache) the underlying OpenBabel `OBMol` object by converting the molecule to an S…
    - `update(val)` — Replace the cached OpenBabel molecule object.
    - `apply_transformation(transf)` — Applies a transformation to the held values
    - `insert_atoms(atoms, coords, where)` — Handles the insertion of new atoms into the structure
    - `delete_atoms(where)` — Handles the deletion from the structure
  - **class `DipoleSurfaceManager`** (PropertyManager)
    - `__init__(mol, surface=None, derivatives=None, polarizability_derivatives=None)`
    - `from_data(mol, data)` — Abstract constructor for building a dipole-surface manager directly from raw data.
    - `surface()` — The dipole surface object, lazily loaded via `load_dipole_surface` if not already set.
    - `numerical_derivatives()` — The numerically-computed dipole derivatives, lazily loaded (together with the analytic derivatives,…
    - `get_derivatives(quiet=False)` — Fetch the analytic dipole derivatives, lazily loading them (together with the numerical derivatives…
    - `derivatives()` — Property getter/setter for the analytic dipole derivatives.
    - `derivatives(derivatives)` — Property getter/setter for the analytic dipole derivatives.
    - `get_pol_derivatives(quiet=False)` — Fetch the polarizability derivatives, lazily loading them via `load_polarizability_derivatives` if…
    - `polarizability_derivatives()` — Property getter/setter for the polarizability derivatives.
    - `polarizability_derivatives(derivatives)` — Property getter/setter for the polarizability derivatives.
    - `load()` — Return whichever representation of the dipole is available: the surface object if one is set, other…
    - `update(val)` — Updates the held values
    - `load_dipole_surface()` — Not currently implemented: general (non-derivative-expansion) dipole surfaces aren't supported yet.
    - `load_dipole_derivatives(file=None, quiet=False)` — Loads dipole derivatives from a file (or from `source_file` if set)
    - `load_polarizability_derivatives(file=None, quiet=False)` — Loads dipole derivatives from a file (or from `source_file` if set)
    - `apply_transformation(transf)` — Apply a coordinate transformation to a copy of this dipole-surface manager, transforming the surfac…
    - `insert_atoms(atoms, coords, where)` — Handles the insertion of new atoms into the structure
    - `delete_atoms(where)` — Handles the deletion from the structure
  - **class `PotentialSurfaceManager`** (PropertyManager)
    - `__init__(mol, surface=None, derivatives=None)`
    - `from_data(mol, data)` — Abstract constructor for building a potential-surface manager directly from raw data.
    - `surface()` — The potential surface object, lazily loaded via `load_potential_surface(self.surface_coords)` if no…
    - `surface_coords()` — Property getter/setter for the coordinate specification used when loading the potential surface (e.…
    - `surface_coords(coords)` — Property getter/setter for the coordinate specification used when loading the potential surface (e.…
    - `get_derivs(quiet=False)` — Fetch the potential-energy derivative tensors, lazily loading them via `load_potential_derivatives`…
    - `derivatives()` — Property getter/setter for the potential-energy derivative tensors.
    - `derivatives(v)` — Property getter/setter for the potential-energy derivative tensors.
    - `force_constants()` — The force-constant (second-derivative/Hessian) tensor from the potential derivative expansion.
    - `load_potential_derivatives(file=None, mode=None, quiet=False)` — Loads potential derivatives from a file (or from `source_file` if set)
    - `load_potential_surface(coordinates)` — Build a `PotentialSurface` object by loading a Gaussian scan log file (`self.mol.source_file`) and,…
    - `load(coordinates=None)` — Load the potential surface using `coordinates` if one is given and no surface is cached yet, then r…
    - `update(val)` — Updates the held values
    - `apply_transformation(transf)` — Apply a coordinate transformation to a copy of this potential-surface manager, transforming the sur…
    - `insert_atoms(atoms, coords, where)` — Handles the insertion of new atoms into the structure
    - `delete_atoms(where)` — Handles the deletion from the structure
  - **class `NormalModesManager`** (PropertyManager)
    - `__init__(mol, normal_modes=None)`
    - `from_data(mol, data)` — Build a `NormalModesManager` from raw normal-mode data of various shapes: an existing `MolecularVib…
    - `set_molecule(mol)` — Rebind this manager (and its stored modes, if any) to a different molecule.
    - `get_modes(quiet=False, allow_compute=True)` — Fetch the normal modes, computing them via `get_normal_modes` if not already cached.
    - `modes()` — :return:
    - `modes(modes)` — Setter for the `modes` property: coerces `modes` into a `MolecularVibrations` object via `construct…
    - `construct_normal_modes(modes)` — Coerce a variety of mode representations (a dict of matrix/kwargs, a raw `np.ndarray`, an object ex…
    - `load()` — Return the currently cached normal modes without attempting to compute them.
    - `update(modes)` — :return:
    - `load_normal_modes(file=None, mode=None, rephase=True, recalculate=False, quiet=False)` — Loads potential derivatives from a file (or from `source_file` if set)
    - `get_normal_modes(quiet=False, compute_force_constants=True, **kwargs)` — Loads normal modes from file or calculates
    - `get_force_constants(compute_force_constants=True, quiet=False)` — Fetch the potential-energy Hessian (force constants), computing it from the molecule's energy evalu…
    - `get_dipole_derivative_based_rephasing(modes, analytic_dipoles, numerical_dipoles, strict=True, allow_swaps=False)` — Derive the sign (and, in limited cases, swap) corrections needed to align a set of normal modes' nu…
    - `get_fchk_normal_mode_rephasing(mol, modes, use_dipoles=False)` — Returns the necessary rephasing to make the numerical dipole derivatives
    - `get_partial_cubic_based_rephasing(modes, partial_cubics, degenerate_freq_threshold=1e-06, equivalent_threshold=0.01, nonzero_threshold=4.5e-06, ignore_single_zeros=False, frequency_scale=True)` — Derive a rephasing (sign/permutation) matrix for a set of nearly-degenerate normal modes using cons…
    - `apply_transformation(transf)` — Apply a coordinate transformation to a copy of this normal-modes manager by embedding its modes thr…
    - `insert_atoms(atoms, coords, where)` — Handles the insertion of new atoms into the structure
    - `delete_atoms(where)` — Handles the deletion from the structure

### `Serializers.py`
  - **class `MoleculePropertyCache`**
    - `__init__(mol, checkpoint=None)`
    - `preserialize()` — Write the molecule's `atoms` and `coords` into the checkpointer so they are present before the rest…
    - `update(vals)` — Bulk-update the checkpointer with multiple key/value pairs.
    - `cached_eval(key, generator, *, condition=None, args=(), kwargs=None)` — Evaluate and cache a value under `key`, delegating entirely to the checkpointer's own `cached_eval`.

### `Transformations.py` — Defines a MolecularTransformation class that uses Coordinerds to describe a physical transformation…
  - **class `MolecularTransformation`** (GeometricTransformation)
    - `apply(mol, shift=True)` — :type mol: AbstractMolecule | np.ndarray | CoordinateTransform

### `Vibrations.py` — Provides classes that manage molecular vibrations
  - **class `MolecularVibrations`**
    - `__init__(molecule, basis, freqs=None, init=None)`
    - `change_mol(mol)` — Rebind this set of vibrations to a different molecule, re-expressing the basis for the new molecule…
    - `basis()` — Property getter/setter for the underlying vibrational basis (a `MolecularNormalModes` object) used…
    - `basis(basis)` — Property getter/setter for the underlying vibrational basis (a `MolecularNormalModes` object) used…
    - `molecule()` — Property getter/setter for the molecule associated with these vibrations.
    - `molecule(mol)` — Property getter/setter for the molecule associated with these vibrations.
    - `freqs()` — Frequencies associated with the vibrations.
    - `coords()` — :return:
    - `displace(displacements=None, amt=0.1, n=1, which=0)` — Displaces along the vibrational mode specified by `which`
    - `visualize(step_size=5, steps=(2, 2), which=0, anim_opts=None, mode='fast', **plot_args)` — :param step_size:
    - `to_widget()` — Build (and cache) an interactive Jupyter widget for browsing through the vibrational modes: a menu…
    - `embed(frame)` — :param frame:
    - `rescale(scaling)` — Multiplies each mode by some scaling factor
    - `rotate(scaling)` — Multiplies each mode by some scaling factor
  - **class `MolecularNormalModes`** (CoordinateSystem)
    > A Coordinerds CoordinateSystem object that manages all of the data needed to
    >  work with normal mode coordinates + some convenience functions for generating and whatnot
    - `__init__(molecule, coeffs, name=None, freqs=None, internal=False, origin=None, basis=None, inverse=None)`
    - `molecule()` — Property getter/setter for the molecule these normal modes belong to.
    - `molecule(mol)` — Property getter/setter for the molecule these normal modes belong to.
    - `change_mol(mol)` — Rebind these normal modes to a different molecule, keeping the mode matrix, name, frequencies, inte…
    - `coords_by_modes()` — Matrix mapping mode displacements back to coordinate displacements (i.e.
    - `modes_by_coords()` — Matrix mapping coordinate displacements onto normal-mode displacements.
    - `to_internals(intcrds=None, dYdR=None, dRdY=None)` — Intended to convert Cartesian-basis normal modes into an internal-coordinate representation using t…
    - `origin()` — Reference geometry the normal modes are expanded about.
    - `embed(frame)` — :param frame:
    - `insert(val, where)` — Inserts values into the appropriate positions in the mode matrix
    - `to_new_modes()` — Converts to the new generalized normal modes
    - `from_new_modes(mol, modes)` — Converts to the new generalized normal modes
    - `from_force_constants(molecule, fcs, *, atoms=None, masses=None, mass_units='AtomicMassUnits', inverse_mass_matrix=False, remove_transrot=True, dimensionless=False, mass_weighted=False, normalize=False, **opts)` — Generates normal modes from the specified force constants

### `Visualizations.py`
  - **class `MoleculePlotter`**
    > Base strategy: holds a molecule + resolved options, owns the shared plotting
    > configuration and every data-derivation method, and dispatches to the right
    > concrete plotter for the resolved mode.
    - `__init__(mol, geometries, *, mode, backend, atoms=None, masses=None, coords=None, bonds=None, **opts)`
    - `coords()` — The coordinates to plot, lazily falling back to `self.mol.coords` if none were supplied explicitly.
    - `bonds()` — The bonds to plot, lazily falling back to `self.mol.bonds` if none were supplied explicitly.
    - `plot_molecule(mol, *geometries, figure=None, return_objects=False, bonds=None, bond_radius=None, atom_radius_scaling=None, atom_style=None, atom_radii=None, atom_text=None, display_atom_numbers=False, radius_type=None, bond_style=None, reconcile_bonds=True, capped_bonds=None, reflectiveness=None, vector_style=None, highlight_atoms=None, highlight_bonds=None, highlight_rings=None, highlight_styles=None, comparison_styles=None, animation_frame_styles=None, mode_vectors=None, mode_vector_origins=None, mode_vector_origin_mode='set', mode_vector_display_cutoff=0.01, principle_axes=None, principle_axes_origin=None, principle_axes_origin_mode='set', principle_axes_style=None, dipole=None, dipole_origin=None, dipole_origin_mode='set', render_multiple_bonds=None, render_fractional_bonds=None, fractional_bond_offset=None, bond_center_radius_offset=None, draw_coords=None, draw_coords_style=None, up_vector=None, multiple_bond_spacing=None, mode=None, backend=None, include_save_buttons=None, objects=False, graphics_class=None, cylinder_class=None, cylinder_options=None, sphere_class=None, sphere_options=None, arrow_class=None, arrow_options=None, line_class=None, line_options=None, disk_class=None, disk_options=None, animate=None, recording_options=None, animation_options=None, jsmol_load_script=None, include_jsmol_script_interface=None, dynamic_loading=None, units='Angstroms', label_style=None, theme='default', theme_function=None, plot_range_padding='auto', annotation_function=None, **plot_ops)` — Top-level entry point for plotting a molecule: gathers the full (very large) set of plotting option…
    - `plot(**new_opts)` — Instance-level plotting entry point: merges any newly supplied options on top of the options stored…
    - `plot_impl(full_opts)` — Abstract rendering hook that concrete plotter subclasses must implement to actually produce a figur…
  - **class `JupyterMoleculePlotter`** (MoleculePlotter)
    - `jupyter_viz()` — Build a `McUtils.Jupyter.MoleculeGraphics` widget for this molecule's atoms, Cartesian coordinates,…
    - `plot_impl(cur_opts)` — Render this plotter's molecule as a Jupyter widget via `jupyter_viz`.
  - **class `JSMolMoleculePlotter`** (MoleculePlotter)
    - `jsmol_viz(xyz=None, animate=False, vibrate=False, script=None, include_script_interface=False, image_size=None, width=None, height=None, figure=None, **etc)` — Build a `JSMol.Applet` widget displaying this molecule, generating the XYZ block from the current c…
    - `plot_impl(full_opts)` — Render this plotter's molecule as a JSMol applet: builds the JSMol-specific plotting options via `_…
  - **class `RDKitMoleculePlotter`** (MoleculePlotter)
    - `plot_impl(full_opts)` — Render this plotter's molecule via RDKit: dispatches to the molecule's `rdmol.plot` (for the `'rdki…
  - **class `Graphics3DMoleculePlotter`** (MoleculePlotter)
    > The main path (`x3d` / `matplotlib3D` / `plotly3D` / `svg3D`). This is the
    > original `plot` body from the `extra_opts` split onward, carried over as-is;
    > `self.<helper>` resolves on the plotter and `self.<mol attr>` is forwarded to
    > the molecule, so nothing in the loop needed rewriting.
    - `plot_impl(full_opts)` — The main 3D rendering path (used for `x3d`/`matplotlib3D`/`plotly3D`/`svg3D`): splits out `Graphics…
  - **class `SVG2DMoleculePlotter`** (MoleculePlotter)
    > Flat 2D structure drawing on a `Graphics` (2D) SVG backend -- the plotter form
    > of the old `Molecule.plot_2d`.
    > *(truncated — see stub for full docstring)*
    - `plot_impl(full_opts)` — The main 2D skeletal-depiction rendering path: splits out `Graphics`-level figure options, resolves…