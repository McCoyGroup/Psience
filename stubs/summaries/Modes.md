### `Coordinates.py` — Handles coordinate manipulation in a unified manner
  - **class `TotalCoordinateConverter`**
    > A converter object that can apply normal mode transformations, non-linear transformations,
    > and generic conversions (and does them in that order)
    - `__init__(converter, inverse, reference_coords, coordinate_expansion, inverse_expansion, modes_matrix, modes_inverse)`
    - `prep_modes(modes_matrix, modes_inverse)` — Fill in whichever of `modes_matrix`/`modes_inverse` wasn't supplied by inverting the other.
    - `prep_expansions(coordinate_expansion, inverse_expansion)` — Fill in whichever of `coordinate_expansion`/`inverse_expansion` wasn't supplied by inverting the ot…
    - `convert(internals)` — Convert reduced internal coordinates into the "total" coordinate representation: splits off the fix…
    - `invert(totals)` — Convert the "total" coordinate representation back into reduced internal coordinates: splits off th…
  - **class `TotalCoordinateSystem`** (CoordinateSystem)
    > A generic coordinate layer to unify coordinate handling,
    > things written in terms of the basic `Coordinerds.CoordinateSet` should be
    > converted over to this format bit-by-bit
    - `__init__(internals_spec, *, reference_coords=None, converter=None, inverse=None, modes_matrix=None, modes_inverse=None, coordinate_expansion=None, inverse_expansion=None, **embedding_options)`
    - `canonicalize_internals(internals_spec, embedding)` — Normalize the `internals_spec` constructor argument into a `(coordinate_system, embedding_options)`…
  - **class `TotalCoordinatesToInternalCoordinateConverter`** (CoordinateSystemConverter)
    - `__init__(totals, internals, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Convert a single frame of total coordinates back to reduced internal coordinates, via the source sy…
    - `convert_many(coords, **kwargs)` — Convert a batch of total coordinates back to reduced internal coordinates, via the source system's…
  - **class `InternalCoordinateToTotalCoordinatesConverter`** (CoordinateSystemConverter)
    - `__init__(totals, internals, **opts)`
    - `types()` — The `(source, target)` coordinate-system type pair this converter handles.
    - `convert(coords, **kw)` — Convert a single frame of reduced internal coordinates to the total-coordinate representation, via…
    - `convert_many(coords, **kwargs)` — Convert a batch of reduced internal coordinates to the total-coordinate representation, via the sou…

### `LocalizedModes.py` — Provides a general class of localized modes using a potentially non-linear
  - **class `LocalizedModes`** (MixtureModes)
    - `__init__(normal_modes, transformation, inverse=None, origin=None, masses=None, freqs=None, mass_weighted=None, frequency_scaled=None, **etc)`
    - `freqs()` — Property getter/setter for the localized modes' frequencies.
    - `freqs(freqs)` — Property getter/setter for the localized modes' frequencies.
    - `mass_weighted()` — Property getter/setter for whether the modes are mass-weighted, delegated to (and required to match…
    - `mass_weighted(new)` — Property getter/setter for whether the modes are mass-weighted, delegated to (and required to match…
    - `g_matrix()` — Property getter/setter for the G-matrix, delegated to `self.base_modes.g_matrix`.
    - `g_matrix(g)` — Property getter/setter for the G-matrix, delegated to `self.base_modes.g_matrix`.
    - `modify(base_modes=None, *, transformation=None, freqs=None, origin=None, masses=None, inverse=None, name=None, mass_weighted=None, frequency_scaled=None, g_matrix=None)` — Build a new `LocalizedModes` with the given fields overridden, defaulting each unspecified field to…
    - `make_mass_weighted(**kwargs)` — Build a mass-weighted version of these localized modes by mass-weighting the underlying base modes…
    - `remove_mass_weighting(**kwargs)` — Build a non-mass-weighted version of these localized modes by removing the mass-weighting from the…
    - `make_frequency_scaled(freqs=None, **kwargs)` — Build a frequency-scaled (dimensionless) version of these localized modes by rescaling the localizi…
    - `remove_frequency_scaling(freqs=None, **kwargs)` — Build a non-frequency-scaled version of these localized modes by undoing the frequency-scaling fact…
    - `compute_hessian(system='modes')` — Compute the Hessian in either the localized-mode basis (via the localizing transformation and the b…
    - `apply_transformation(transformation, inverse=None, **opts)` — Apply an additional linear transformation on top of the existing localizing transformation, returni…
    - `get_complement(concatenate=False)` — Build the mode set complementary to this localized set (i.e.

### `MixtureModes.py` — Provides support for handling modes that arise from
  - **class `MixtureModes`** (CoordinateSystem)
    > A `McUtils.Coordinerds.CoordinateSystem` object that expresses coordinates as
    > a rotation on some base set of coordinates with some associated frequencies.
    - `__init__(basis, coeffs, freqs=None, origin=None, masses=None, inverse=None, mass_weighted=False, frequency_scaled=False, g_matrix=None, name=None)`
    - `to_state(serializer=None)` — Serialize this mode set's essential data (basis, matrix, inverse, frequencies, masses, mass-weighti…
    - `from_state(data, serializer=None)` — Reconstruct a `MixtureModes` from a previously serialized state dict.
    - `prep_modes(modes)` — Coerce a variety of mode representations (an already-built `MixtureModes`-like object, a dict of ma…
    - `modify(matrix=None, *, freqs=None, origin=None, masses=None, inverse=None, name=None, mass_weighted=None, frequency_scaled=None, g_matrix=None)` — Build a new `MixtureModes` (of the same concrete subclass) with the given fields overridden, defaul…
    - `rotate(rot, in_place=False)` — Intended to rotate the mode set by a given rotation.
    - `transform(tf, inv=None, origin=None)` — Intended to apply a linear transformation (and its inverse) to the mode set.
    - `cartesian_modes()` — Whether the modes are expressed relative to a Cartesian (as opposed to internal-coordinate) origin,…
    - `embed_coords(carts)` — Convert a batch of Cartesian displacement structures into mode coordinates, by subtracting the refe…
    - `unembed_coords(mode_coords)` — Convert a batch of mode coordinates back into Cartesian structures, by projecting through the modes…
    - `total_transformation()` — The full (possibly multi-term, nonlinear) forward coordinate-expansion tensors this mode set was co…
    - `inverse_transformation()` — The (lazily computed and cached) inverse of `total_transformation`, i.e.
    - `embed_derivs(derivs)` — Re-express a set of derivative tensors (with respect to the base coordinates) in terms of mode coor…
    - `unembed_derivs(derivs)` — Re-express a set of derivative tensors (with respect to mode coordinates) back in terms of the base…
    - `is_cartesian()` — Whether the modes are expressed over a Cartesian coordinate space, either inferred from the mode-ma…
    - `coords_by_modes()` — **LLM Docstring**
    - `modes_by_coords()` — **LLM Docstring**
    - `compute_local_transformations(f, g)` — Compute the pair of diagonal rescaling transformations (for the force-constant matrix and its inver…
    - `compute_local_hessian(f, g)` — Compute the rescaled ("local mode") force-constant matrix from full `f`/`g` matrices, using the dia…
    - `compute_local_gmatrix(f, g)` — Compute the rescaled ("local mode") G-matrix from full `f`/`g` matrices, using the diagonal rescali…
    - `compute_hessian(system='modes')` — Compute the (signed-frequency-squared) Hessian matrix in either the mode basis or the underlying co…
    - `compute_gmatrix(system='modes', return_fractional=False)` — Compute the G-matrix in either the mode basis or the underlying coordinate basis, using whichever i…
    - `compute_freqs()` — Recompute the vibrational frequencies for this mode set from its coordinate-basis Hessian and G-mat…
    - `local_hessian()` — The "local mode" (diagonally rescaled) force-constant matrix for this mode set.
    - `local_gmatrix()` — The "local mode" (diagonally rescaled) G-matrix for this mode set.
    - `local_freqs()` — The diagonal entries of the local-mode Hessian, giving an approximate per-mode "local" force consta…
    - `local_mode_transformations()` — The pair of diagonal rescaling transformations mapping between this mode set's coordinate-basis Hes…
    - `get_nearest_mode_transform(alternate_modes, mass_weighted=None, atoms=None, maximum_similarity=True, unitarize=True, masses=None)` — Find the transformation (and its inverse) that best aligns this mode set with an externally supplie…
    - `get_normal_modes(f_matrix, mass_spec, remove_transrot=True, dimensionless=False, mass_weighted=None, zero_freq_cutoff=None, return_gmatrix=False, projector=None)` — Compute ordinary harmonic normal modes from a force-constant matrix and mass specification (masses,…
    - `get_projected_localized_mode_transformation(projectors, masses=None, origin=None, localization_type=None, allow_mode_mixing=False, maximum_similarity=False, unitarize=True, zero_freq_cutoff=None, orthogonal_projection=False, project_zero_gmatrix_modes=True, project_zero_gmatrix_cutoff=1e-08, atoms=None)` — Shared driver behind the various coordinate/atom/fragment-localized-mode constructors: diagonalizes…
    - `get_atom_localized_mode_transformation(atoms, masses=None, origin=None, localization_type='ned', allow_mode_mixing=False, maximum_similarity=False, orthogonal_projection=False, unitarize=True, zero_freq_cutoff=None)` — Build a localized-mode transformation that concentrates vibrational character onto (or, if `orthogo…
    - `get_fragment_localized_mode_transformation(fragment, masses=None, origin=None, localization_type='ned', allow_mode_mixing=True, maximum_similarity=False, orthogonal_projection=False, unitarize=True, zero_freq_cutoff=None, **etc)` — Build a localized-mode transformation for a molecular fragment (a set of atoms), via `get_atom_loca…
    - `get_coordinate_projected_localized_mode_transformation(coordinate_constraints, atoms=None, masses=None, origin=None, localization_type='ned', allow_mode_mixing=False, maximum_similarity=False, orthogonal_projection=True, unitarize=True)` — Build a localized-mode transformation that concentrates vibrational character along a set of intern…
    - `get_internal_localized_mode_transformation(expansion_coordinates, fixed_atoms=None, mass_weighted=False, project_transrot=True, atoms=None, maximum_similarity=False, orthogonal_projection=False, projection=False, allow_mode_mixing=False, unitarize=True, origin=None, masses=None, localization_type='ned')` — Build a localized-mode transformation aligned with a set of internal-coordinate displacement direct…
    - `get_displacement_localized_mode_transformation(mode_blocks=None, atoms=None, mass_weighted=True, unitarize=True, **maximizer_opts)` — Build a unitary localized-mode transformation by iteratively (Jacobi-style) rotating blocks of mode…
    - `get_mass_scaled_mode_transformation(mass_scaling, *, atoms, localization_cutoff=0.8, num_modes=None, project_transrot=False, unitarize=True, **diag_opts)` — Build a localized-mode transformation by artificially scaling the masses of a set of atoms (making…
    - **class `LocalizationMethods`** (enum.Enum)
      > Real access pattern: LocalizationMethods.<MemberName> (this is an enum with 8 members, e.g. LocalizationMethods.MaximumSimilarity == 'target_modes'). Collapsed into a dict below purely for compactness -- do not index it as a dict in real code:
    - `localizer_dispatch()` — The mapping from `LocalizationMethods` value to the `(constructor_method, primary_argument_name)` p…
    - `localize(method=None, *, atoms=None, fragment=None, target_modes=None, internals=None, mode_blocks=None, coordinate_constraints=None, projections=None, reorthogonalize=None, mass_scaling=None, unitarize=True, allow_mode_mixing=False, project_zero_gmatrix_modes=None, project_zero_gmatrix_cutoff=1e-08, **opts)` — Top-level entry point for building a `LocalizedModes` object: infers which localization method to u…
    - `make_mass_weighted(masses=None)` — Build a mass-weighted version of this mode set by rescaling the mode/inverse matrices and origin th…
    - `remove_mass_weighting(masses=None)` — Build a non-mass-weighted version of this mode set by undoing the mass-weighting rescaling; returns…
    - `make_frequency_scaled(freqs=None)` — Build a frequency-scaled (dimensionless) version of this mode set by rescaling the mode/inverse mat…
    - `remove_frequency_scaling(freqs=None)` — Build a non-frequency-scaled version of this mode set by undoing the frequency-scaling rescaling; r…
    - `make_dimensionless(freqs=None, masses=None)` — Build a fully dimensionless version of this mode set: mass-weighted and frequency-scaled.
    - `make_dimensioned(freqs=None, masses=None)` — Build a fully dimensioned version of this mode set: not mass-weighted and not frequency-scaled.
    - `apply_projection(proj, project_transrot=True, masses=None, origin=None)` — Apply an arbitrary projection matrix to the mode/inverse matrices (optionally first combining it wi…
    - `apply_constraints(coordinate_constraints, atoms=None, masses=None, origin=None, orthogonal_projection=True)` — Build a new mode set with the given internal coordinates constrained (projected out or onto, depend…
    - `apply_transformation(tf, **opts)` — Apply an arbitrary linear transformation to this mode set, returning the result as a `LocalizedMode…
    - `make_oblique()` — Build an "oblique" mode representation (see `ObliqueModeGenerator`) that makes the mode transformat…

### `NormalModes.py`
  - **class `NormalModes`** (MixtureModes)
    - `__init__(basis, coeffs, freqs=None, origin=None, masses=None, inverse=None, name=None, mass_weighted=False, frequency_scaled=False, g_matrix=None)`
    - `from_fg(basis, f_matrix, mass_spec, remove_transrot=True, dimensionless=False, zero_freq_cutoff=None, mass_weighted=None, origin=None, projector=None, **opts)` — Generates normal modes from the specified F and G matrices
    - `from_molecule(mol, dimensionless=False, use_internals=None, potential_derivatives=None, project_transrot=True, zero_freq_cutoff=None, masses=None, energy_evaluator=None, **opts)` — Build normal modes for a molecule, dispatching to `from_fg` with either the internal-coordinate Hes…
    - `get_reaction_path_modes(grad, zero_gradient_cutoff=None, return_status=False, **kwargs)` — Build reaction-path-following normal modes from this mode set and a gradient vector, via `ReactionP…
  - **class `ReactionPathModes`** (NormalModes)
    - `get_rp_modes(gradient, f_matrix, mass_spec, remove_transrot=True, dimensionless=False, mass_weighted=None, zero_freq_cutoff=None, return_gmatrix=False, projector=None, zero_gradient_cutoff=None, use_max_gradient_cutoff=True, gradient_check_transformation=None, return_indices=False)` — Compute reaction-path-following vibrational modes from a gradient and force-constant/mass specifica…
    - `from_grad_fg(basis, gradient, f_matrix, mass_spec, remove_transrot=True, dimensionless=False, zero_freq_cutoff=None, mass_weighted=None, origin=None, projector=None, zero_gradient_cutoff=None, gradient_check_transformation=None, return_status=False, **opts)` — Generates normal modes from the specified F and G matrices
    - `from_molecule(mol, dimensionless=False, use_internals=None, potential_derivatives=None, project_transrot=True, zero_freq_cutoff=None, masses=None, zero_gradient_cutoff=None, return_status=False, gradient_check_internals=None, gradient_check_transformation=None, **opts)` — Build reaction-path-following normal modes for a molecule at a (generally non-stationary) point, us…
    - `from_modes_and_grad(modes, grad, zero_gradient_cutoff=None, use_max_gradient_cutoff=True, return_status=False, mass_weighted=None, **projection_opts)` — Given an existing mode set and a gradient vector, build reaction-path-following modes by localizing…

### `ObliqueModes.py`
  - **class `ObliqueModeGenerator`**
    - `__init__(f, g, dimensionless=False, sel=None, frequency_scaled=True)`
    - `from_molecule(mol, dimensionless=True, sel=None, use_internals=None, frequency_scaled=True)` — Build an `ObliqueModeGenerator` for a molecule, using its internal-coordinate potential derivatives…
    - `run(scaling_type='normal', remove_frequency_scaling=True)` — Apply the oblique-mode rotation (or its inverse, if `scaling_type='inverse'`) to transform `f` and…